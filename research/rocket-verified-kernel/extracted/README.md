# Extracted OCaml

This directory is for OCaml generated by Rocq extraction.

Rules:

- generated files are never hand-edited;
- each generated file must be reproducible from rocq/theories/Extract.v;
- record the Rocq version and extraction command in docs/WORKER_NOTE.md;
- extracted code is research-only in Experiment 1;
- do not wire it into production Tethers;
- a thin handwritten adapter may live under ../tests/ or a future ../ocaml-adapter/ directory, but it must be clearly outside the proved kernel.

Expected first artifact:

rocket_path_kernel.ml
