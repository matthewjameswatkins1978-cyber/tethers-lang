# Worker Note

Task: `TETHERS-0.3-P1-R1G-RERUN — Final P1 Acceptance Gate (Rerun)`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `a0bdead29b89f76b41f3350d014e02f5f060e9a9`

Implementation checkpoint: `169056bb6b53c0e51cdc15a517e4d55534df258a`

## Requested outcome

Rerun the final P1 acceptance gate after the bounded J23C3 stale-test correction (R1G-FIX). Verification + closeout only. No implementation changes.

## History

- First R1G run was BLOCKED on stale J23C3 assertion (`assert_eq!` at line 226 expected equal digests for different scope content)
- R1G-FIX corrected two stale expectations: `assert_eq!` → `assert_ne!` and "enablement scope does not match supplied scope" → "enablement pins are stale"
- R1G-FIX accepted at `a0bdead29b89f76b41f3350d014e02f5f060e9a9`
- This R1G-RERUN passed all 8 mandatory gates from the corrected base

## Changes made

- `docs/CURRENT_CLINE_TASK.md` — updated from R1G-FIX COMPLETE to R1G-RERUN task packet with all required sections
- No production code changes
- No test changes

## Decisions and assumptions

1. J23C3 test now correctly uses `assert_ne!` for different scope content and expects "enablement pins are stale" error — both corrections verified correct by the full suite PASS
2. Reused valid evidence (engine fixtures, MCP transcripts, fixture validator) remains valid because R1G-FIX changed only 2 lines in J23C3 test + docs
3. All provider-specific references found in `src/` files reside inside `#[cfg(test)]` blocks only

## Evidence

### Gate results

| Gate | Result | Detail |
|---|---|---|
| Clean starting state | PASS | HEAD == a0bdead29b89f76b41f3350d014e02f5f060e9a9; untracked file removed; genuinely clean |
| No-knowledge gate | PASS | 19 matches in `src/` all inside `#[cfg(test)]` blocks (plug_command.rs:1142-1265, package.rs:754/805, enablement.rs:415) |
| Retired-delivery gate | PASS | Zero matches for `__TETHERS_PDF_\|__TETHERS_FILE_\|TETHERS_PDF_\|TETHERS_FILE_` in `tethers-0.1/host-rust/src` |
| Dependency gate | PASS | Zero diff in Cargo.toml / Cargo.lock vs `c0fd57780156bee023d8dcff884737ea470d096c` |
| `cargo clippy --all-targets --all-features --locked` | PASS | Compiled without error; warnings only |
| Engine fixtures | PASS — REUSED VALID EVIDENCE | 29 PASS; no OCaml files changed during P1 |
| MCP transcripts | PASS — REUSED VALID EVIDENCE | 15 PASS; R1G-FIX changed only assertions/messages in J23C3 + docs |
| Fixture validator | PASS — REUSED VALID EVIDENCE | 46 JSON + 30 JSONL PASS; no fixture data or production behaviour changed |
| `just verify-agent` | PASS | Full suite: 1667 tests run, 1667 passed, 2 skipped |
| `git diff --check` | PASS | Clean, no whitespace errors |

### Full Rust suite counts

- `cargo test`: 1667 tests run, 1667 passed, 2 skipped
- Nextest: 1667 passed

### J23C3 evidence

```
PASS [   3.849s] (1563/1667) tethers-reference-host::j23c3_installed_pdf_execution installed_pdf_plug_lifecycle
```

### Task packet checker

```
PASS task packet consistency (control-v1/IN_PROGRESS): base a0bdead, HEAD 169056b
```

## P1 acceptance criteria reconciliation (14/14 YES)

1. File/PDF OperationalScope enum gone — YES (generic `OperationalScopeEvidence` serves all providers)
2. Generic lifecycle has no subject-specific scope branching — YES (all paths use generic scope shapes)
3. Plug enable accepts Plug-declared scope shapes — YES (r1e_synthetic_unrelated_plug, j23c1, j24d tests pass)
4. Scope schema pinned through package/candidate/install/enablement — YES (schema digest carried end to end)
5. Scope evidence canonical + tamper-evident — YES (j23c1 tamper tests pass, j23c3 digest mismatch caught)
6. Installed launch uses generic scope delivery — YES (j23c3 installed launch succeeds with correct scope)
7. Retired File/PDF delivery gone from generic core — YES (retired-delivery gate clean)
8. Language semantics unchanged — YES (no spec or parser changes)
9. No dependency change — YES (dependency gate clean vs c0fd577)
10. Required verification passes — YES (clippy, cargo test, just verify-agent)
11. `just verify-agent` passes — YES (1667/1667)
12. `git diff --check` clean — YES
13. Branch pushed / remote equals local / worktree clean — YES
14. No-knowledge gate clean — YES (all matches in test blocks)

### Completed P1 repair chain

- R1A schema evidence pinning
- R1B generic validation + nested duplicate handling
- R1C canonical-directory semantics
- R1D reference-provider scope obedience
- R1E unrelated synthetic Plug via real plug-enable CLI
- R1F current generic PDF conformance
- R1G-FIX stale J23C3 proof correction
- R1G-RERUN final gate rerun (this task)

## Discoveries

1. The first R1G run exposed a J23C3 test defect where `assert_eq!` was used instead of `assert_ne!` for digests of scopes with different content. The test also had a stale expected error message.
2. R1G-FIX corrected both assertions with exactly 2 lines changed. The second correction (error message) was necessary because the pin check catches digest mismatch before content comparison.
3. All gates from the corrected base pass consistently.

## Remaining risks

None known within P1 scope.

## Smallest next action

Lucy independent GitHub review and FINAL P1 ACCEPTANCE.

## References

- Base commit: `a0bdead29b89f76b41f3350d014e02f5f060e9a9` (R1G-FIX accepted)
- Verification checkpoint: `169056bb6b53c0e51cdc15a517e4d55534df258a`
- Dependency reference: `c0fd57780156bee023d8dcff884737ea470d096c`
- Branch: `feature/0.3-p1-r1g-final-gate-rerun`
- `src/operational_scope.rs:66-81` — integrity digest includes canonical_scope_json
- `src/launch_profile.rs:237-254` — pin check before content comparison
- `tests/j23c3_installed_pdf_execution.rs` — corrected J23C3 test
- R1G-FIX worker note: `docs/worker-notes/2026-08-10-0.3-p1-r1g-fix-j23c3-scope-digest.md`
