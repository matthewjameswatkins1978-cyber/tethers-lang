# Worker Note — TETHERS-0.3-P3 PDF Tools Reference Plug Crucible

Task: `TETHERS-0.3-P3 — PDF Tools Reference Plug Crucible`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Codex`

Status: `COMPLETE`

Base commit: `06bcb29d36522f0b75bd24eac7c4b66e49f8ea33`

Implementation checkpoint: `fcf22bff911393869d8dd560efeee1442a50b119`

## Requested outcome

Move PDF semantics and the provider into a standalone reference Plug, then
prove public authoring and real installed execution through generic host
machinery.

## Changes made

- Added the standalone `reference-plugs/pdf-tools/provider-rust` provider and
  committed reference author material under `reference-plugs/pdf-tools/author`.
- Added generic supervised-child adoption, normal retained-session admission,
  discovery comparison, and `InstalledProviderExecutor` without PDF branches.
- Removed host-owned PDF implementation, provider binary, manifest, and old
  split PDF tests; added one ignored public/installed-execution P3 crucible.
- Replaced generic lifecycle tests' retired PDF package fixture identities with
  the shared neutral fixture while retaining their lifecycle assertions.
- Added `test-pdf-reference` to `verify-agent`.
- Lucy's final-review correction routes the real installed PDF action through
  `execute_enabled_installed_action`, proving policy, intent, replay, outcome,
  Result Anchor, and Trail handling around the generic provider executor.

## Decisions and assumptions

- Reused the existing supervisor, MCP session, discovery comparison, and
  capability dispatch seams; no second MCP or supervision implementation was
  introduced.
- The standalone provider has no `tethers-reference-host` dependency and only
  `serde`, `serde_json`, and `sha2` direct dependencies.
- The initial `just verify-agent` wrapper run failed only at Nextest with exit
  110 after 63 seconds. Its exact Nextest stage immediately passed on direct
  retry: 1670 passed, 4 skipped. The authorised wrapper rerun then passed.

## Evidence

- `pwsh -NoProfile -File scripts/check-dev-tools.ps1` — PASS.
- Initial packet checker — PASS: `control-v1/IN_PROGRESS`.
- Focused neutral-fixture test binaries — PASS, including `j23c1`, `j24c`,
  `j24d`, `j24e`, `j24f`, `j24j`, `j24k2`, `j24l2`, recovery evidence, and
  relevant library tests.
- `just test-pdf-reference` — PASS: standalone provider fmt/test/build and both
  ignored P3 public and real installed-execution crucible tests.
- Host warnings-denied check — PASS.
- Final searches for host PDF production references, provider host dependency,
  and old generic fixture builders/imports — PASS: no matches.
- First `just verify-agent` run — transient FAIL at Nextest (exit 110).
- Direct retry of the exact Nextest stage — PASS: 1670 passed, 4 skipped.
- Authorised `just verify-agent` rerun — PASS: Cargo tests, Nextest (1670
  passed, 4 skipped), agent tooling, dependency policy/advisories, provider
  checks, and both ignored P3 crucible tests.
- Final-review correction `just verify-agent` run — PASS: Cargo tests, Nextest
  (1670 passed, 4 skipped), agent tooling, dependency policy/advisories,
  provider checks, and both ignored P3 crucible tests.
- `git diff --check` and complete implementation diff inspection — PASS.

## Discoveries

- Generic lifecycle tests had borrowed PDF fixture identities beyond the direct
  builder imports. They were completed as one bounded neutral-fixture migration.
- The stale recovery fixture manifest name was `pdf-inspect-v1.json`; updating
  it to `fixture-ping.json` preserved its capability-drift assertion.
- The existing generic host seam accepts the normal resolved capability,
  enabled binding snapshot, planned action, Trail path, and provisioned replay
  root without any production or PDF-specific adaptation.

## Remaining risks

- P3 implementation is complete but awaits Lucy's independent review and
  acceptance. P4 has not started.

## Smallest next action

Lucy performs the independent GitHub review/acceptance of the implementation
checkpoint and its docs-only closeout.

## References

- Base: `06bcb29d36522f0b75bd24eac7c4b66e49f8ea33`
- Original P3 checkpoint: `907eaa75b17a4441806df342af30fd5ffd9c8ea7`
- Final-review correction checkpoint: `fcf22bff911393869d8dd560efeee1442a50b119`
- Branch: `feature/0.3-p3-pdf-reference-plug`
- `reference-plugs/pdf-tools/`
- `tethers-0.1/host-rust/tests/p3_pdf_reference_plug.rs`
