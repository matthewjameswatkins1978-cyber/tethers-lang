# Worker Note

Task: `RELEASE-0.2.2-PREP — Tethers 0.2.2 Release Candidate`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `5108b06f1f694d6523d5f3f342c08ca0f9b9cbc1`

Implementation checkpoint: `c6ea1e1652fa2785a1f06e0ace2fcd5e826ee6ec`

## Requested outcome

Prepare the complete Tethers 0.2.2 release candidate: version identity, Cargo
single-source-of-truth, fixture migration, release notes, README front door,
and Foundation completion recording. One fully verified release-candidate
checkpoint for Lucy review.

## Changes made

### Version inventory
Classified every `0.2.0`/`v0.2.0` occurrence as CURRENT, HISTORICAL, or
COMPATIBILITY FIXTURE. Only authorised surfaces were migrated.

### Phase 2 — Single source of product version truth
- `tethers-0.1/host-rust/Cargo.toml`: version `0.2.0` → `0.2.2`.
- `tethers-0.1/host-rust/Cargo.lock`: local `tethers-reference-host` entry
  only; no dependency version, checksum, source, or graph change.
- `tethers-0.1/host-rust/src/cli.rs`: replaced hard-coded `version = "0.2.0"`
  with `version = env!("CARGO_PKG_VERSION")`. Cargo.toml is now the
  authoritative Rust host/CLI product-version source. No new dependency, no
  envelope/exit change.

### Phase 3 — Explicit version fixture migration
- `docs/foundation-pass/fixtures/cli-output/version.txt`: recaptured from real
  0.2.2 binary via `--version`. Only difference: `0.2.0` → `0.2.2`. Schema
  (`tethers.cli/1`), status (`invalid_cli_usage`), exit code (2), error code
  (`INVALID_CLI_USAGE`), and envelope structure unchanged.
- `docs/foundation-pass/FIXTURE_MANIFEST.md`: added post-Foundation migration
  record. Preserved F1 provenance.
- `docs/foundation-pass/MODULE_DEPENDENCY_MAP.md`: crate version `v0.2.0` →
  `v0.2.2`.

### Phase 4 — GitHub README front door
- `README.md`: new opening introduces Tethers to first-time visitors.
  "Tethers is a small deterministic automation language..." with signature
  "Make things happen. Keep the receipts." Current release section describes
  0.2.2 as release candidate. Maintained existing repository map sections.

### Phase 5 — Release notes
- `docs/releases/v0.2.2.md`: created with release candidate status, Foundation
  highlights, version history, known limitations, supported environment. Used
  v0.2.0.md as structural precedent. Tag and published commit marked pending.

### Phase 6 — Foundation and current project truth
- `docs/CURRENT_GOAL.md`: Foundation F1-F10 COMPLETE/ACCEPTED, F10 accepted
  SHA recorded, active increment is 0.2.2 release preparation, main not
  advanced.
- `docs/PROJECT_DASHBOARD.md`: updated for 0.2.2 prep active task, Foundation
  accepted checkpoint recorded.

## Decisions and assumptions

- `env!("CARGO_PKG_VERSION")` reads from Cargo.toml at build time, making it the
  single source of truth without adding a dependency.
- Cargo.lock updated by direct line edit (not `cargo generate-lockfile`) to
  avoid unintended dependency-version changes. The surgical edit changed only
  the local `tethers-reference-host` version from `0.2.0` to `0.2.2`.
- Existing OCaml engine switch (`D:\The Next Thing\Tethers Lang\tethers-0.1\engine-ocaml`)
  reused for engine tests; no new switch created.
- MCP transcript fixtures contain `"version":"0.2.0"` in OCaml engine
  `serverInfo` — these were not migrated because the OCaml engine was not
  rebuilt with a version change. The engine fixture output is unchanged.

## Evidence

### Focused verification
- Cargo metadata: `"version":"0.2.2"`. PASS.
- Release binary `--version`: reports `0.2.2` in unchanged envelope.
  Exit code 2, schema `tethers.cli/1`, status `invalid_cli_usage`,
  error code `INVALID_CLI_USAGE`. PASS.
- Captured output vs fixture: byte-for-byte match. PASS.
- CLI tests (j13a_cli): 29 passed, 0 failed. PASS.
- Cargo.lock diff: only local package identity line changed. PASS.
- Final `0.2.0` sweep: only deliberately historical references remain in
  README ("previous published release was Tethers 0.2.0") and task-packet
  scope text. PASS.
- Zero hard-coded `"0.2.2"` in cli.rs; version derives from
  `env!("CARGO_PKG_VERSION")`. PASS.
- `just fmt`: PASS.
- `git diff --check`: clean.

### Release-candidate verification (against c6ea1e1)
- `just verify-agent`: PASS. 1592 tests passed, 2 skipped.
- Engine fixture checks (`test-engine.ps1`): 29 PASS.
- MCP transcript cases (`test-mcp-transcripts.ps1`): 15 PASS.
- Fixture validator (`check-fixtures.ps1`): 46 JSON + 30 JSONL PASS.

## Publication evidence

- Branch: `release/v0.2.2-prep`
- Implementation checkpoint: `c6ea1e1652fa2785a1f06e0ace2fcd5e826ee6ec`
- Closeout follows corrected SHA workflow.

## Discoveries

- `cargo generate-lockfile` updated 12 dependency versions beyond the local
  package. The correct approach for this task was a surgical line edit of
  Cargo.lock, which preserved all existing dependency versions, checksums,
  and sources.

## Remaining risks

None known within packet scope. Publication requires Lucy review and acceptance
before advancing `main`, creating the `v0.2.2` tag, or cutting a GitHub Release.

## Smallest next action

Lucy reviews the pushed 0.2.2 release-candidate evidence and decides on
publication.

## References

- `tethers-0.1/host-rust/Cargo.toml`
- `tethers-0.1/host-rust/Cargo.lock`
- `tethers-0.1/host-rust/src/cli.rs`
- `docs/foundation-pass/fixtures/cli-output/version.txt`
- `docs/foundation-pass/FIXTURE_MANIFEST.md`
- `docs/foundation-pass/MODULE_DEPENDENCY_MAP.md`
- `README.md`
- `docs/releases/v0.2.2.md`
- `docs/CURRENT_GOAL.md`
- `docs/PROJECT_DASHBOARD.md`
- `docs/CURRENT_CLINE_TASK.md`
