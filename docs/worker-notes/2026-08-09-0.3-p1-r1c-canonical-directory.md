# Worker Note

Task: `TETHERS-0.3-P1-R1C — Canonical Operational Scope Directories`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `f18d211523de953d260417e67abbadf766412037`

Implementation checkpoint: `bb4ba228f8812703bb06bcf9970de42f4a9eee44`

Previous closeout HEAD: `942282e98761ee39f7d683d696ba72941b259835`

The implementation checkpoint SHA is the original R1C code commit. The previous closeout HEAD was the first closeout commit that added the task packet and initial worker note.

## Requested outcome

Implement `x-tethers-path: canonical-directory` with real filesystem path hardening. Replace `validate_operational_scope()` with `validate_and_canonicalize_operational_scope()` in `run_enable()`, validate before and after canonicalisation, reuse existing `m3_store::verify_chain()` and `m3_store::reject_reparse()` primitives, and store the canonical scope (not the supplied path) in `OperationalScopeEvidence`.

## Changes made

### R1C (implementation checkpoint bb4ba22)

- `tethers-0.1/host-rust/src/validation.rs`: Added `validate_and_canonicalize_operational_scope()` (public), `canonicalize_directory_path()` (private), and `canonicalize_scope_value()` (private). Added `use std::{fs, path::Path}`. Added 10 focused R1C tests.
- `tethers-0.1/host-rust/src/plug_command.rs`: Changed `run_enable()` to call `validate_and_canonicalize_operational_scope()` instead of `validate_operational_scope()`, and pass the returned canonical scope to `OperationalScopeEvidence::create()`. Added 5 focused R1C tests including 2 Windows junction/reparse tests.

### R1C-FIX (this correction)

- `tethers-0.1/host-rust/src/validation.rs`:
  - Marked `validate_operational_scope()` with `#[cfg(test)]` to eliminate the dead_code warning introduced by R1C when production use was replaced.
  - Added `r1c_additional_properties_schema_canonicalised` test proving schema-valued `additionalProperties` canonicalisation.
- `docs/CURRENT_CLINE_TASK.md`: Updated to reflect R1C-FIX correction.
- `docs/worker-notes/2026-08-09-0.3-p1-r1c-canonical-directory.md`: This corrected note.

## Decisions and assumptions

- Path hardening follows the same pattern as `m3_store::StoreRoot::open()`: `verify_chain` before canonicalisation, `fs::canonicalize`, then `verify_chain` + `reject_reparse` after.
- `canonicalize_scope_value` walks schema and scope in parallel. It processes `properties`, `items`, and schema-valued `additionalProperties`. Scalars and unannotated nodes pass through unchanged.
- UTF-8 conversion via `Path::to_str()` fails closed if the canonical path is not representable as valid UTF-8 (unlikely on Windows with typical directory names).
- The first validation pass catches type errors before canonicalisation attempts a filesystem call.
- `validate_operational_scope` is now `#[cfg(test)]` — it has no production callers since R1C.

## Evidence

### R1C-FIX verification

`cargo check --all-targets --all-features --locked`: **0 warnings** — the dead_code warning is eliminated.

### 15 focused R1C tests

```
r1c_relative_path_refused                                        ok
r1c_nonexistent_directory_refused                                ok
r1c_non_string_value_refused                                     ok
r1c_nested_property_canonicalised                                ok
r1c_array_items_canonicalised                                    ok
r1c_canonical_result_passes_revalidation                         ok
r1c_existing_file_is_refused                                     ok
r1c_redundant_components_resolve_to_same_canonical               ok
r1c_without_annotation_passes_unchanged                          ok
r1c_additional_properties_schema_canonicalised                   ok  ← NEW
r1c_evidence_stores_canonical_path_not_original                  ok
r1c_schema_digest_unchanged_by_canonicalization                  ok
r1c_deterministic_canonical_evidence                             ok
r1c_junction_directory_is_refused [Windows]                      ok
r1c_path_through_junction_ancestor_is_refused [Windows]          ok
```

### R1B regression: 14/14 pass

### Full lib test suite

`cargo test --lib`: **1372 passed, 0 failed, 2 ignored**

### Formatting

`cargo fmt --all -- --check`: clean

### Git diff

`git diff --check`: clean

### Task packet checker

Pending (run after closeout commit).

## Publication evidence

Branch: `feature/0.3-p1-r1c-canonical-directory`

Local HEAD and remote HEAD equality confirmed after push.

## Discoveries

- The `canonicalize_directory_path` function naturally integrates with the existing `m3_store` path hardening primitives without modification.
- `verify_chain` iterates all ancestors up to root, handling `NotFound` gracefully for non-existent intermediate components.
- The dead_code warning was introduced by R1C, not pre-existing. It is now eliminated via `#[cfg(test)]`.

## Remaining risks

None known within packet scope.

## Smallest next action

Lucy review and acceptance of R1C-FIX.

## References

- `tethers-0.1/host-rust/src/validation.rs:75-78` — test-only `validate_operational_scope`
- `tethers-0.1/host-rust/src/validation.rs:83-218` — canonicalisation implementation
- `tethers-0.1/host-rust/src/plug_command.rs:858-870` — run_enable integration
- `docs/CURRENT_CLINE_TASK.md` — complete task packet
- Branch: `feature/0.3-p1-r1c-canonical-directory`
- Implementation checkpoint: `bb4ba228f8812703bb06bcf9970de42f4a9eee44`
- Previous closeout HEAD: `942282e98761ee39f7d683d696ba72941b259835`
