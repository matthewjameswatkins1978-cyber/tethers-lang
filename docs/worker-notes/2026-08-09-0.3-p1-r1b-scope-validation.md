Task: `TETHERS-0.3-P1-R1B — Enforce Operational Scope Schema`
Task packet: `docs/CURRENT_CLINE_TASK.md`
Owner: `OpenCode`
Status: `COMPLETE`
Base commit: `6da96996c303338474623b0d0e774271549b4ed4` (accepted R1A HEAD)
Implementation checkpoint: `5e69ca08e3ffc1cf33453a5fae2f35c52d78d7be`

## Requested outcome

Close the P1 gap where `run_enable()` carried the operational-scope schema through the evidence chain but never validated the supplied scope against it. Also fix nested duplicate-key parsing inside scope files.

## Changes made

1. **`validation.rs`** — Added `"x-tethers-path"` to the SUPPORTED keyword list in `reject_unsupported_keywords()`. Added `validate_operational_scope()` public function as a thin wrapper around `validate_against_schema()`.

2. **`plug_command.rs`** — Added `ScopeValue` newtype with a custom `Deserialize` impl that rejects duplicate object keys at every depth. Updated `GenericScopeRequest.scope` from `serde_json::Value` to `ScopeValue`. Updated `parse_scope_file()` and `run_enable()` to use `scope_request.scope.0`.

3. **`plug_command.rs` - `run_enable()`** — Replaced the digest-only check with a coherent-pair check requiring both `operational_scope_schema` and `operational_scope_schema_digest` from the installed record. After parsing the scope file, calls `validate_operational_scope(&schema, &scope_request.scope.0)` before creating `OperationalScopeEvidence`.

4. **10 focused tests** (in `plug_command.rs` test module):
   - `r1b_valid_scope_passes`
   - `r1b_wrong_property_type_fails`
   - `r1b_missing_required_property_fails`
   - `r1b_unknown_property_fails_when_additional_properties_false`
   - `r1b_numeric_minimum_fails`
   - `r1b_numeric_maximum_fails`
   - `r1b_unsupported_schema_keyword_fails`
   - `r1b_nested_duplicate_scope_key_fails`
   - `r1b_valid_scope_creates_deterministic_evidence`
   - `r1b_schema_digest_from_installed_evidence_remains_unchanged`

## Decisions and assumptions

- `x-tethers-path` recognised in the supported keyword list so existing PDF/File tool schemas (which use it) pass validation. Semantics deferred to R1C.
- No arbitrary `x-tethers-*` — only the exact keyword. Other extensions remain rejected.
- `ScopeValue` uses a recursive visitor pattern that rejects duplicates at every object depth, matching the existing duplicate-detection pattern in `GenericScopeRequest`.
- `#[derive(Debug)]` added to `ScopeValue` and `GenericScopeRequest` for test diagnostics.

## Evidence

- `cargo check --all-targets --all-features --locked` — PASS (0 errors)
- `cargo fmt --all -- --check` — PASS (formatted, then rechecked clean)
- `cargo clippy --all-targets --all-features --locked` — PASS (all warnings pre-existing)
- `git diff --check` — PASS
- 10 focused R1B tests — PASS
- Full lib test suite (`cargo test --lib --all-features --locked`) — 1353 passed, 0 failed, 2 ignored (Unix-only)
- No dependency changes

## Discoveries

1. rustfmt automatically reformatted 3 lines in `installed.rs`, 5 in `installation_recovery_evidence_tests.rs`, and 5 in `installation_publication_preparation_tests.rs` (all R1A code). These are mechanical line-reflow only, within authorised paths.
2. `ScopeValue` must implement `Debug` so that `Result<GenericScopeRequest, M3Error>` can be formatted in test assertions.

## Remaining risks

1. `x-tethers-path` canonicalisation not yet enforced (R1C)
2. Synthetic plug proof not yet written (R1C)
3. j23c2 conformance tests fail on removed `__TETHERS_*` placeholder behaviour (outstanding P1 evidence)

## Smallest next action

R1C: implement `x-tethers-path: "canonical-directory"` filesystem semantics.

## References

- `tethers-0.1/host-rust/src/validation.rs:55-63` — `validate_operational_scope()`
- `tethers-0.1/host-rust/src/validation.rs:471-493` — `reject_unsupported_keywords()` with `x-tethers-path`
- `tethers-0.1/host-rust/src/plug_command.rs:501-568` — `ScopeValue` and `GenericScopeRequest`
- `tethers-0.1/host-rust/src/plug_command.rs:838-862` — `run_enable()` validation flow
- `tethers-0.1/host-rust/src/plug_command.rs:990-1180` — focused R1B tests
