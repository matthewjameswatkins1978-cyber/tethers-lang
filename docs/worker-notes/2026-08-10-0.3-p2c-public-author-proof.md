# Worker Note

Task: `TETHERS-0.3-P2C — End-to-End Public Author Proof + Final P2 Gate`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `061a57d4bd48e59cae2d496b889834df7fe54418`

Implementation checkpoint: `4c32b96446e7ae3e20d2994056d0fd435dcc32f3`

## Requested outcome

Prove the complete public Plug-author journey (pack → inspect → conform) using
only the public CLI. Run the expensive final P2 verification gate (`just
verify-agent`). Record all evidence. No production code changes.

## Changes made

6 files total: 2 new, 4 modified docs.

- `tethers-0.1/host-rust/tests/p2c_public_author_journey.rs` (new): one
  coherent end-to-end test exercising the full public author journey
- `docs/worker-notes/2026-08-10-0.3-p2c-public-author-proof.md` (new): this note
- `docs/CURRENT_CLINE_TASK.md` (updated): P2C task packet with acceptance criteria
- `docs/CURRENT_GOAL.md` (updated): P2B accepted, P2C complete, P3 next
- `docs/ROAD_TO_0_3.md` (updated): P2B accepted, P2C complete
- `docs/PROJECT_DASHBOARD.md` (updated): P2C complete, awaiting Lucy review

Zero production source (`src/`) changes. Zero dependency changes (Cargo.toml
and Cargo.lock unchanged from `061a57d4bd48e59cae2d496b889834df7fe54418`).

## Decisions and assumptions

- Chose a single coherent test over a large matrix, following the packet
  directive. P2A and P2B already own their detailed negative matrices.
- Used synthetic identities (`example.public-author-proof`,
  `tethers-stdio-fixture`, `fixture.ping@1`) as directed.
- Author plug.json uses `payloads` (not `payload_index`). Capability declaration
  omits `manifest_digest`. Source manifest omits top-level `digest`.
- Used `m3_fixture_provider.exe` (compiled binary) copied into the author source
  tree as the ordinary provider executable.
- Provider marker path set via `--provider-marker` in launch arguments, placed
  outside the author source directory.
- Conform with dedicated TEMP/TMP parent for workspace cleanup proof (scans
  only that parent, not machine-wide temp).
- No `plug stage`, `plug install`, or `plug enable` in the journey.

## Evidence

### P2C focused test

```
cargo test --locked --test p2c_public_author_journey
```
Result: PASS — 1 test, 1 passed, 0 failed (1.67s)

Verifies all 10 acceptance criteria:
1. Author source tree with synthetic identity, `payloads`, no `manifest_digest`
2. Public pack: exit 0, correct identities, digest fields, one JSON envelope line
3. Public inspect: exit 0, identity unchanged, manifest digest exists, digest continuity
4. Conform without approval: exit 5, `approval_required`, `conformance_execution_approval_required`
5. Approved conform: exit 0, disposition=passed, isolated=false, limitation present, evidence fields
6. Provider marker exists after approved conform
7. No `tethers-p2b-conform-*` directories remain in dedicated TEMP/TMP parent
8. Digest continuity: `pack == inspect == conform semantic_package_digest`
9. Source and package bytes unchanged throughout journey
10. Public output contains no internal paths, raw stderr, or `M3_SECRET_CANARY`

### Cargo fmt

```
cargo fmt --all -- --check
```
Result: PASS (no diff)

### git diff --check

Result: PASS (only LF/CRLF conversion warning for working copy, no real issues)

### Cargo clippy

```
cargo clippy --all-targets --all-features --locked
```
Result: PASS — 0 errors, all warnings are pre-existing in existing production and test files. No new warnings from P2C.

### Overall linting verification (during verify-agent)

```
cargo fmt 1.1s PASS
cargo check 12.7s PASS
```

### just verify-agent (repository root)

All components PASS:
- `task-packet` 0.9s PASS
- `cargo-fmt` 1.1s PASS
- `cargo-check` 12.7s PASS
- `cargo-test` 96.1s PASS — 1714 passed, 0 failed, 2 ignored (30 test binaries, recovered via rerun)
- `agent-tools` 6.1s PASS (rust-analyzer 1.97.1, rust-toolchain 1.97.1, nextest 0.9.140)
- `deps-policy` 0.9s PASS
- `deps-advisories` 12.0s PASS
- `nextest` 64.4s PASS — 1714 tests run, 1714 passed, 2 skipped

### Dependency gate

Cargo.toml: unchanged from P2B base `061a57d4bd48e59cae2d496b889834df7fe54418`.
Cargo.lock: unchanged from P2B base.

### Task packet checker (IN_PROGRESS state)

```
pwsh -NoProfile -File .github/scripts/check-tethers-task-packet.ps1
```
Result: PASS (IN_PROGRESS allowed, all structural checks pass)

## P2 acceptance reconciliation

- P2A: public deterministic pack — YES
- Existing public inspect: read-only package evidence — YES
- P2B: public supervised conform — YES
- P2C: real author pack → inspect → conform journey demonstrated — YES
- Public workflow: `plug pack`, `plug inspect`, `plug conform` — coherent surface proven
- All verification gates pass — YES

## Discoveries

None. The public author journey worked exactly as designed. No production code
gaps were found. The pack/inspect/conform pipeline is deterministic and
immutable end-to-end.

## Remaining risks

No known risks within P2C scope. P2C was proof-focused with zero production
changes. All gates pass cleanly. P2 awaits Lucy final independent acceptance.

## Smallest next action

Lucy performs final independent P2 acceptance review. Upon acceptance, P3 (PDF
Tools Reference Plug Crucible).

## References

- Base: `061a57d4bd48e59cae2d496b889834df7fe54418` (P2B FINAL ACCEPTED)
- Implementation checkpoint: `4c32b96446e7ae3e20d2994056d0fd435dcc32f3`
- Branch: `feature/0.3-p2c-public-author-proof`
- Task packet: `docs/CURRENT_CLINE_TASK.md`
- P2A test: `tethers-0.1/host-rust/tests/p2a_plug_pack_cli.rs`
- P2B test: `tethers-0.1/host-rust/tests/p2b_plug_conform_cli.rs`
- P2C test: `tethers-0.1/host-rust/tests/p2c_public_author_journey.rs`
