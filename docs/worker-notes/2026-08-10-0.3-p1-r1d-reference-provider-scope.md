# Worker Note

Task: `TETHERS-0.3-P1-R1D — Reference Providers Obey Generic Scope`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Codex`

Status: `COMPLETE`

Base commit: `20cdb463e9c84f35b6d70997916305a1443cfd1d`

Implementation checkpoint: `86ab5f7a785b63feb2ddba6d80746776fc5a10e8`

## Requested outcome

Make both reference providers consume their existing generic Operational Scope exactly in normal installed operation, refusing absent, malformed, incomplete, wrong-typed, or out-of-bounds configuration. Preserve only the bounded TEMP fallback for exact `TETHERS_CONFORMANCE == "1"`.

## Changes made

- `tethers-0.1/host-rust/src/bin/file_tools_provider.rs`: replaced per-field current-directory fallback with one fallible scope parse; requires all three roots and `max_content_bytes`, then applies the existing `FileScope` bound.
- `tethers-0.1/host-rust/src/bin/pdf_tools_provider.rs`: replaced split startup resolution with one fallible scope parse; normal mode requires exact `max_bytes` and exact conformance is the only maximum-bound fallback.
- `tethers-0.1/host-rust/tests/m4_file_tools.rs`: the native provider test now supplies operational scope through the installed-launch environment contract.

## Decisions and assumptions

- `scope_from_configuration` is an inner `Result` boundary for direct tests; `main` remains the sole process-exit boundary.
- The providers retain only subject-specific scope parsing. Generic scope validation, evidence, enablement, installed records, and launch machinery are unchanged.
- Conformance detection is an exact equality check: only `Some("1")` is conformance mode.

## Evidence

- `cargo test --locked --bin file_tools_provider --bin pdf_tools_provider` — PASS: File Tools 14 passed; PDF 9 passed.
- `cargo test --locked --test m4_file_tools --test j23a_pdf_provider` — PASS: File Tools 4 passed; PDF 7 passed.
- `cargo check --all-targets --all-features --locked` — PASS: clean.
- `cargo fmt --all -- --check` — PASS: clean.
- `git diff --check` — PASS: clean.

## Publication evidence

Pending closeout commit and normal push to `origin/feature/0.3-p1-r1d-reference-provider-scope`.

## Discoveries

- The existing File Tools native provider test passed unused command-line arguments rather than the provider's actual environment scope contract; it now exercises the installed-launch contract directly.
- Existing generic conformance launch configuration already supplies `TETHERS_CONFORMANCE=1` and a bounded TEMP scratch directory, so the provider-only exception did not require host changes.

## Remaining risks

- The pre-existing J23C2 placeholder/conformance behaviour was intentionally not changed or re-run; this packet explicitly excludes that repair.

## Smallest next action

Perform the independent R1D review; do not begin another R job without a separately authorised packet.

## References

- Base: `20cdb463e9c84f35b6d70997916305a1443cfd1d`
- Implementation checkpoint: `86ab5f7a785b63feb2ddba6d80746776fc5a10e8`
- Branch: `feature/0.3-p1-r1d-reference-provider-scope`
