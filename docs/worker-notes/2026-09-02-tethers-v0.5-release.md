# Tethers 0.5 practical release closeout

Task: `Tethers 0.5 — Practical release finishing packet`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Codex`

Status: `COMPLETE`

Base commit: `21bb7442fa9f8442db98e193eb4954096f356678`

Implementation checkpoint: `6cdd7a678be5a23ead56d83da9778c18fc749566`

## Requested outcome

Produce a practical Tethers 0.5 release on top of current `origin/main`, with
frozen V2 identity unchanged, an exact Rocket portfolio and permanent
exhaustive reference, reconciled Agent Essentials providers and manuals,
reproducible Windows/Linux packaging, and bounded release evidence.

## Changes made

- Added the Rocket V3 portfolio API with named B2 success-path, R3-2 refined
  exact-leaf, frozen V2 exact-search, and exhaustive-reference routes.
- Added the runtime-only complexity valve: a bounded V2 search that declines
  a case calls the exhaustive reference engine; it never emits a different
  identity. Added a permanent exact Enc_V2 leaf certificate and accepted
  success-path/origin-walk Rocket implementations.
- Added portfolio differential/metamorphic tests and retained the existing V2
  reference, IR, symmetry, and generated-corpus checks.
- Integrated Agent Essentials discovery, workspace provider, coding provider,
  reference Plug manifests, packers, and the AI-first quickstart.
- Added deterministic 0.5 packaging and GitHub Actions for Windows x64 and
  Linux x64 musl, while leaving the portable 0.2.2 line and recorded hashes
  unchanged.
- Added product, Tether Set, cold-agent, benchmark, install, and security
  boundary documentation.

## Decisions and assumptions

Frozen Enc_V2 bytes and ProgramDigest V2 remain the sole semantic authority.
The Rocket portfolio is additive at this release boundary; it does not switch
the live host/Core wire path away from the already accepted V2 authority.
Existing V2 IR pruning and memoisation remain the exact broader-shape route.
The blocked R3-3B3A/B3B/B3C research notes are not promoted as production
theorems. Linux packaging is a CI claim until a Linux runner proves it.

## Evidence

- `scripts/check-dev-tools.ps1`: PASS.
- Packet checker: READY, IN_PROGRESS, and final COMPLETE all pass.
- OCaml authorised switch `D:\The Next Thing\Tethers Lang\tethers-0.1\engine-ocaml`:
  `dune build @all` PASS; full `dune runtest --force` PASS; focused Rocket
  portfolio `41/41` PASS; existing Rocket model `214/214`, refinement
  `4807/4807`, and all lowerer/validator/adapter/plan/wire suites pass.
- Existing V2 differential evidence: generated corpus `5000/5000` valid,
  `0` mismatches, `16` archetypes; reference and production suites pass.
- Rocket chain-1000 evidence: 1000 committed choices, 1003 feasibility
  checks, 3 rejected choices, and 0 complete permutations enumerated.
- Rust formatting and locked checks pass. Release-relevant serial test targets
  pass: 3 + 4 + 4 + 7 + 13 + 1 tests. The unconstrained full Windows test
  run exposed existing concurrent terminal-test interference; an isolated
  failing test passes, so that run is not reported as a clean parallel gate.
- Reference workspace/coding packages pack successfully with 11/12
  capabilities and semantic digests
  `sha256:e5c20ed9465ea5bd406ea3c4f1d28f6c44fe6c512848af4949af4d4fe4376a10`
  and `sha256:8dc7ae3fb97b7e390c22ee0bd76b4854534f1214a91458187fba0fbfb5cc9641`;
  both conformance runs pass 6/6 in `m3-generic-1`.
- Deterministic Windows bundle repeat hash:
  `66315C81E56374D5FD51ED80AF8E7D1EFAC1432B0642174A344F18E2F6CA4E83`.
- Existing portable 0.2.2 checksum record remains
  `E19D67DE625612D19A0EA5D3FD7DF96EB6EDCB2EDAA6CA18543783CCE9CBEB4D` for
  Windows and `02089B741737EE35C85CE179030CCADEF22FB207A4C3CCC0D2719B710142C7E0`
  for Linux x64 musl.
- Cold-agent transcript proves JSON discovery, empty-state truthfulness,
  trusted package inspection, and supervised conformance without invented
  capability state.

## Discoveries

The current host and portable package versions remain `0.2.2`; `0.5` is the
product release line, not a silent protocol-version rewrite. The old source
checkout is dirty and was left untouched. The research branches contain useful
evidence but no additional production theorem safe to merge into this cut.

## Remaining risks

The Linux asset still requires the tagged GitHub Actions workflow. Physical
installation acceptance, signatures, and the final GitHub release URL are
external publication facts and must not be inferred locally. The full parallel
Windows test harness has a pre-existing interference problem in terminal tests;
release-relevant targets are green when isolated/serialised.

## Smallest next action

Create the normal `tethers-v0.5.0` tag from the closeout commit, push it, and
allow the release workflow to prove both platform assets before announcing the
release.

## References

- `README.md`
- `QUICKSTART.md`
- `docs/TETHERS_0_5_RELEASE.md`
- `docs/evidence/tethers-0.5-cold-agent-transcript.md`
- `docs/evidence/tethers-0.5-rocket-benchmark.md`
- `tethers-0.1/portable-rust/SHA256SUMS-0.2.2`
