# Worker Note

Task: `TETHERS-0.3-P4 — Plug Author Manual`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `e23030ad5e9820373133b25222680194af967c39`

Implementation checkpoint: `8b90ce76b70b33276f6b633828cfc782064bb792`

## Requested outcome

Write the first complete public Plug-authoring manual using only interfaces and
behaviour proven by P1–P3, using the real PDF Tools reference Plug as the
example, then record the small 0.3 project-state cleanup and the agreed
delegation/blocking-rule workflow correction.

## Changes made

- Added `docs/PLUG_AUTHORING.md` — the canonical public author manual, covering
  the full 15-section journey: what a Plug is, authoring mental model, author
  source tree, `plug.json`, capability manifest, provider contract, Operational
  Scope Evidence, building the provider, pack-source assembly, `plug pack`,
  `plug inspect`, `plug conform` (both paths), complete PDF Tools walkthrough,
  common mistakes, and author checklist.
- `docs/ROAD_TO_0_3.md` — recorded P3 FINAL ACCEPTED at
  `e23030ad5e9820373133b25222680194af967c39` (correction checkpoint
  `fcf22bff911393869d8dd560efeee1442a50b119`), P4 active, P5 next and not started.
- `docs/CURRENT_GOAL.md` — same state update; replaced the stale "P3 is next"
  increment text and last-accepted-result section.
- `docs/PROJECT_DASHBOARD.md` — updated milestone, active task, last accepted
  result, and next route to reflect P3 accepted and P4 active.
- `docs/AGENT_WORKFLOW.md` — added the "Delegation and BLOCKED" subsection under
  Failure And Stop Rules with the delegation/blocking-rule principle, local-solve
  examples, BLOCKED threshold, and corrected repeated-failure rule.
- `AGENTS.md` — added a compact matching reminder to the Working Rules.
- `docs/CURRENT_CLINE_TASK.md` — replaced the P3 packet with this P4 packet.

No production Rust/OCaml, CLI, Cargo, or semantics changed. Documentation and
project-control work only.

## Decisions and assumptions

- Manual documents only P1–P3-proven surfaces; no future APIs are described.
- The PDF Tools reference Plug is used as the concrete example throughout.
- Conformance is described as supervised, non-isolated, non-installing, and
  non-trust-creating, including the `--allow-non-isolated-supervised-execution`
  flag and the default `approval_required` refusal.
- Every documented command and field name was cross-checked against `cli.rs`
  and the P2/P3 CLI tests before writing.

## Evidence

- Toolchain preflight `pwsh -NoProfile -File scripts/check-dev-tools.ps1`: PASS
  (rg, fd, jq, yq, gh, just, git, pwsh all present).
- Task packet checker (IN_PROGRESS state): PASS `control-v1/IN_PROGRESS`.
- Documented commands cross-checked against `tethers-0.1/host-rust/src/cli.rs`
  (`plug pack --source/--output`, `plug inspect --package`, `plug conform
  --package` + `--allow-non-isolated-supervised-execution`).
- Documented field names cross-checked against
  `reference-plugs/pdf-tools/author/plug.json` and
  `author/manifests/pdf-inspect-v1.json` (package identity, protocol binding,
  provider, capabilities, `payloads`, scope schema, manifest fields, binding).
- Pack/inspect output structures cross-checked against `tests/p2a_plug_pack_cli.rs`
  (`payloads`→`payload_index`, `semantic_package_digest`, `semantic_digest`,
  `manifest_digest`, `provider_id`, `capability_count`, `raw_archive_digest`).
- Conform behaviour cross-checked against `tests/p3_pdf_reference_plug.rs`
  (default refusal: exit code 5, `approval_required`,
  `conformance_execution_approval_required`; approved run: disposition `passed`,
  `isolated: false`, "not isolated" limitation).
- `git diff --check`: clean. Full diff/status inspected; no production source
  changed (`git diff --name-only` matched no `.rs`/Cargo files).
- Final task-packet checker (COMPLETE state) reports `control-v1/COMPLETE`.

## Publication evidence

- Branch pushed normally to `origin/feature/0.3-p4-plug-author-manual`.
- Full remote HEAD SHA after push: `ff80954b599e0b78f61d305f273e7077f736fc50`.
- Local `HEAD == remote HEAD`: confirmed (equal).
- Final `git status --short --branch`: clean.

## Discoveries

None.

## Remaining risks

None known within packet scope.

## Smallest next action

Lucy reviews the pushed P4 evidence and, if accepted, compiles the P5
fresh-agent authoring-proof task.

## References

- `docs/PLUG_AUTHORING.md`
- `reference-plugs/pdf-tools/author/plug.json`
- `reference-plugs/pdf-tools/author/manifests/pdf-inspect-v1.json`
- `reference-plugs/pdf-tools/provider-rust/`
- `tethers-0.1/host-rust/src/cli.rs`
- `tethers-0.1/host-rust/tests/p3_pdf_reference_plug.rs`
- `tethers-0.1/host-rust/tests/p2a_plug_pack_cli.rs`
- `justfile`
- `docs/ROAD_TO_0_3.md`, `docs/CURRENT_GOAL.md`, `docs/PROJECT_DASHBOARD.md`
- `docs/AGENT_WORKFLOW.md`, `AGENTS.md`
