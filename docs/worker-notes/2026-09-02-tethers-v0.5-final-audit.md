# Tethers 0.5 final forgotten-things audit

Task: `Tethers 0.5 — Final Forgotten-Things Audit and Release Closure`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Codex`

Status: `IN_PROGRESS`

Base commit: `31d5e39a1e3505880e9a98cd8c650b3cf112b16d`

Implementation checkpoint: `b69cca6ed9409c68295d7492253b353696dc4758`

## Requested outcome

Finish the practical Tethers 0.5 release without changing frozen V2
semantics: close the first-class benchmarker, public preview, bounded Trail
receipt, real starter examples, cold-agent proof, platform evidence, and
tagged publication. Keep archive, HTTP, SQLite, structured-data, and
read-only system orientation deferred where they require new contracts or
authority.

## Changes made

- Added the Rocket-aware `tethers-bench` Dune target with fixed workloads,
  human output, stable JSON, before/after comparison, environment context,
  resource counters, and portfolio/reference parity guards.
- Added public read-only `preview` and bounded `trail --receipt` surfaces.
- Added three runnable ordinary Tether Set examples and front-door guidance.
- Completed the J14A public cold-agent path through discovery, inspection,
  preview, harmless execution, Result Anchor, Trail inspection, and receipt;
  exact replay remains duplicate-effect safe.
- Corrected stale J14A/Cargo.lock fixture evidence and repaired the release
  workflow's Linux test selection so Windows-only P2B conformance remains on
  Windows while Linux runs platform-neutral release gates.
- Added package inclusion for the benchmarker manual and starter examples.

## Decisions and assumptions

Frozen Enc_V2, ProgramDigest V2, the existing authority/trust model, and the
permanent exhaustive Rocket reference remain unchanged. The benchmarker is a
repository-first source/Dune/manual tool in 0.5; the existing package binaries
remain the native host and portable workbench assets. Structured-data and
read-only system-orientation were re-checked against the actual manifests and
providers and were deferred because neither has an existing bounded contract.

## Evidence

- Startup tool and packet gates passed; the exact fresh worktree was based on
  `31d5e39a1e3505880e9a98cd8c650b3cf112b16d`.
- `tethers-bench` produced `tethers.benchmarker/1`, five cases, three
  comparison rows, native backend context, resource counters, and all-case
  portfolio/reference parity.
- OCaml V2/Rocket regression, differential/metamorphic, origin-walk,
  success-path, and exact-chain suites passed; the Rocket differential corpus
  reported zero mismatches.
- J14A passed 7 cases and 121 assertions.
- Locked Rust formatting/checks and serial release-surface tests passed.
- Windows package/extraction smoke passed. Local package SHA-256:
  `1A68268874575E62ADD708602C9C0891F9E5918CA6840F13D9C24A617C2FC9D1`.
- Prior hosted Linux failure `33637303122` was diagnosed as the Windows-only
  P2B conformance fixture in the Linux matrix; the workflow correction is in
  the implementation checkpoint.

## Remaining risks

The fresh tagged workflow must prove the corrected Linux package and both
hosted assets. The final GitHub release, remote-equal proof, and final asset
hashes are not claimed until the hosted run completes. Physical installation
acceptance remains outside repository evidence.

## Smallest next action

Commit this evidence closure, push the branch normally, create the new
`tethers-v0.5.6` tag, and let the release workflow prove publication. Then
record its exact run, assets, hashes, remote equality, and clean worktree before
marking the packet `COMPLETE`.

## References

- `docs/evidence/tethers-0.5-final-audit-matrix.md`
- `docs/evidence/tethers-0.5-final-audit-closeout.md`
- `docs/evidence/tethers-0.5-cold-agent-transcript.md`
- `docs/TETHERS_BENCHMARKER.md`
- `docs/TETHERS_0_5_RELEASE.md`
