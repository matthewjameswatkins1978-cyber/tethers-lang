# Worker Note

Task: `F1 - Baseline and debt inventory`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `24428139807cac0adeb0b62264547e61ca809d16`

Implementation checkpoint: `2d932260e40465f3935f6d14db10aade1fc4a55c`

## Checkpoints

| Checkpoint | SHA | Purpose |
|---|---|---|
| Preparation | `158422a54bede77ea59d6d08fe1fcdb5ed21d499` | Initial F1 branch state with programme preamble and packet correction |
| F1 evidence | `2d932260e40465f3935f6d14db10aade1fc4a55c` | All F1 artifacts complete; ready for verification matrix |
| Final documentation | `d0776112ace170019ad077d5a655b26c657bf913` | After verification matrix; packet marked COMPLETE; branch pushed |

Note: `fe6609688a4dbbef996528c1077d039e02dba486` (from original round) was the previous evidence checkpoint, replaced by `2d93226`.

## Requested outcome

Produce a documentation-and-evidence-only baseline for the Foundation Pass: validate the provisional pre-pass baseline, record measured repository facts, create independent literal compatibility fixtures, and classify all discovered debt without repairing it.

## Changes made (correction round)

### Packet and status

- Set `Status: IN_PROGRESS` in both task packet and worker note.

### Fixture replacement

Replaced 5 non-fixture placeholder files with literal artifacts from accepted baseline sources:

| Replaced (non-fixture) | Replacement (literal fixture) | Source |
|---|---|---|
| `success-check-template.json` | `success-plug-list.json` | Binary capture: `plug list --host-data-root <tmp>` at baseline, exit 0 |
| `trail-shape.json` | `trail-matched.json` + `trail-not-matched.json` | Exact copies of `tethers-0.1/protocol/expected-response.json` and `protocol/cases/false-condition/expected-response.json` (committed at baseline) |
| `digest-shape.txt` | `replay-digest-chain.txt` | Constructed from `src/replay.rs` Claim/Generation schema at baseline with JCS-canonicalized SHA-256 values |
| `installation-outcomes/status-codes.txt` | `j24l-install-success.json`, `j24l-install-already-complete.json`, `j24l-install-refusal.json` | Constructed from J24L2/J24K test assertions at baseline with M3 golden schema UUIDs |
| `recovery-states/transitions.txt` | `m3-installation-state.json`, `m2-candidate-record.json` | Exact copies of `tethers-0.1/host-rust/fixtures/m3/m3-schema-golden-v1.json` and `fixtures/m2/candidate-record-v1.json` (committed at baseline) |

Total fixtures: 17 files across 7 categories (was 13).

### Persistence inventory rebuild

- Rebuilt `PERSISTENCE_INVENTORY.md` with required 10-column format per store
- Reduced to 4 durable classes: Immutable Atomic Record, Replaceable Current-State Record, Append-Only Causal Log, Multi-Step Intent/Recovery Journal
- Separated non-durable in-memory stores into appendix (6 stores)
- Each store appears in exactly one class
- Corrected Trail classification: append-only JSONL with `sync_data()`, NOT write-then-rename
- Identified `m3_store.rs` (StoreRoot) as shared persistence layer for 7 stores
- Marked Installation Recovery Plan as read-only planner, not a store
- Routed UNVERIFIED file/directory durability claims to F3b

### Debt ledger correction

- Consolidated D2+M4 (missing .mli) into single M4 as Maintainability debt
- Reclassified D3 (dead `max_line_bytes`) to M5: Maintainability
- Reclassified D4 (dead `FailingResultAnchorWriter`) to M6: Maintainability
- Reclassified A2 (no OCaml tests) to M7: Maintainability
- Reclassified A3 (test modules in src/) to M8: Maintainability
- Added M9: test infrastructure `pub(crate)` visibility
- Retained D1 as Confirmed defect with explicit qualification: nondeterministic test behaviour; production root cause unconfirmed
- Marked live-stderr as F2 candidate only (no F1 reproduction evidence)
- New counts: 1 Confirmed defect, 1 Contract ambiguity, 9 Maintainability, 2 Performance, 3 Documentation, 1 F2 candidate = 17 total

### Baseline transcript correction

- Described environment as existing clean worktree, not a fresh clone
- Recorded that timing was NOT CAPTURED for major commands
- Stated cold/warm distinction for test runs
- Noted OCaml engine binaries are pre-built artefacts, not rebuilt during F1

### Test inventory correction

- Stated inventory is representative (mapped to contracts), not exhaustive (not all 1,254 tests individually mapped)

### FIXTURE_MANIFEST correction

- Updated with per-fixture provenance: owner/contract, exact source command or source fixture/test, accepted baseline SHA, capture method, exit code, normalisation notes, review purpose
- Distinguishes binary captures from protocol fixture copies from test assertion constructions

## Evidence

Final serial verification matrix at HEAD `2d932260e40465f3935f6d14db10aade1fc4a55c`:

| # | Command | Result |
|---|---|---|
| 1 | `git fetch origin --prune` | PASS |
| 2 | `git rev-parse origin/main` | PASS (`24428139807cac0adeb0b62264547e61ca809d16`) |
| 3 | `git rev-parse HEAD` | PASS (`2d932260e40465f3935f6d14db10aade1fc4a55c`) |
| 4 | `git status --short --branch` | PASS (ahead 1, only worker note dirty) |
| 5 | `rustup show` | PASS (1.97.1 active) |
| 6 | `cargo --version` | PASS (1.97.1) |
| 7 | `cargo fmt --all -- --check` | PASS (no violations) |
| 8 | `cargo check --all-targets --all-features --locked` | PASS (compiles, warnings only) |
| 9 | `cargo test --all-targets --all-features --locked` | PASS (1254 passed, 0 failed, 2 ignored, including previously-flaky M3 test) |
| 10 | `cargo clippy --all-targets --all-features --locked -- -W clippy::all` | PASS (warnings only, 0 errors) |
| 11 | `just verify` | PASS |
| 12 | `just verify-agent` | PASS (1504 passed, 2 skipped) |
| 13 | `pwsh -NoProfile -File .github/scripts/check-tethers-task-packet.ps1` | PASS (control-v1/IN_PROGRESS) |
| 14 | `git diff --check origin/main...HEAD` | PASS (no whitespace errors) |
| 15 | `git diff --name-only origin/main...HEAD` | PASS (28 files, all under `docs/`) |
| 16 | `git status --short --branch` | PASS (only worker note dirty) |

Test totals: 1254 unit + 250 integration = 1504 tests. All passed warm. Previously-flaky M3 test also passed on this run.
Warning totals: ~137 Clippy warning occurrences. 0 compile errors.

## Branch diff

28 files changed, all under `docs/`. Zero production code, test code, Cargo.lock, or dependency changes. Confirmed by `git diff --name-only origin/main...HEAD`.

## Discoveries

1. Flaky test: `m3_windows_handle_allow_list_excludes_unrelated_inheritable_handle` — nondeterministic, probable timing-dependent Windows handle enumeration race.
2. Fully JSON-enveloped CLI: even `--help` returns a JSON envelope with `exit_code: 2`, `status: invalid_cli_usage`.
3. Exit code convention: CLI uses exit code 2 for invalid usage, 0 for success.
4. OCaml engine has no direct tests: all testing via Rust host or PowerShell scripts.
5. No `.mli` files: OCaml modules expose full internals.
6. `application.rs` at 8,260 lines: largest file, mixed concerns.
7. Replay Ledger is the ONLY store with directory-entry durability (dual `FlushFileBuffers`).
8. Trail uses append+sync, not write-then-rename. No per-line digest or integrity footer.
9. `m3_store.rs` (StoreRoot) is shared persistence layer for 7 stores.
10. Local Anchor Admission Store lacks `verify_chain()` on its store root.

## Remaining risks

- Flaky M3 test: passed warm during this matrix run; cold behaviour unpredictable.
- Directory durability not independently verified (routed to F3b).
- OCaml toolchain not available in this worktree.
- FileTrail: no path safety, no per-line integrity, no corruption detection.

## Smallest next action

Await Lucy's review of branch `foundation/f1-baseline` at the final documentation checkpoint. If accepted, Lucy compiles F2.

## References

- Base: `24428139807cac0adeb0b62264547e61ca809d16` (`origin/main`)
- Prep checkpoint: `158422a54bede77ea59d6d08fe1fcdb5ed21d499`
- F1 evidence checkpoint: `2d932260e40465f3935f6d14db10aade1fc4a55c`
- Final documentation checkpoint: `d0776112ace170019ad077d5a655b26c657bf913`
- Branch tip: `d0776112ace170019ad077d5a655b26c657bf913`
- Task packet: `docs/CURRENT_CLINE_TASK.md`
- Architecture: `docs/architecture/TETHERS_FOUNDATION_PASS.md`
- F1 artifacts: `docs/foundation-pass/`
