# Worker Note

Task: `F1 - Baseline and debt inventory`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `IN_PROGRESS`

Base commit: `24428139807cac0adeb0b62264547e61ca809d16`

Implementation checkpoint: (to be set after F1 evidence commit)

## Checkpoints

| Checkpoint | SHA | Purpose |
|---|---|---|
| Preparation | `158422a54bede77ea59d6d08fe1fcdb5ed21d499` | Initial F1 branch state with programme preamble and packet correction |
| F1 evidence | (to be set) | All F1 artifacts complete; ready for verification matrix |
| Final documentation | (to be set) | After verification matrix; packet marked COMPLETE; branch pushed |

## Requested outcome

Produce a documentation-and-evidence-only baseline for the Foundation Pass: validate the provisional pre-pass baseline, record measured repository facts, create independent literal compatibility fixtures, and classify all discovered debt without repairing it.

## Changes made (correction round)

### Packet and status

- Set `Status: IN_PROGRESS` in both task packet and worker note.

### Fixture replacement

Replaced 5 non-fixture placeholder files with literal artifacts from accepted baseline sources:

| Replaced (non-fixture) | Replacement (literal fixture) | Source |
|---|---|---|
| `success-check-template.json` | `success-plug-list.json` | Binary capture: `plug list --host-data-root <tmp>` at baseline, exit 0 |
| `trail-shape.json` | `trail-matched.json` + `trail-not-matched.json` | Exact copies of `tethers-0.1/protocol/expected-response.json` and `protocol/cases/false-condition/expected-response.json` (committed at baseline) |
| `digest-shape.txt` | `replay-digest-chain.txt` | Constructed from `src/replay.rs` Claim/Generation schema at baseline with JCS-canonicalized SHA-256 values |
| `installation-outcomes/status-codes.txt` | `j24l-install-success.json`, `j24l-install-already-complete.json`, `j24l-install-refusal.json` | Constructed from J24L2/J24K test assertions at baseline with M3 golden schema UUIDs |
| `recovery-states/transitions.txt` | `m3-installation-state.json`, `m2-candidate-record.json` | Exact copies of `tethers-0.1/host-rust/fixtures/m3/m3-schema-golden-v1.json` and `fixtures/m2/candidate-record-v1.json` (committed at baseline) |

Total fixtures: 17 files across 7 categories (was 13).

### Persistence inventory rebuild

- Rebuilt `PERSISTENCE_INVENTORY.md` with required 10-column format per store
- Reduced to 4 durable classes: Immutable Atomic Record, Replaceable Current-State Record, Append-Only Causal Log, Multi-Step Intent/Recovery Journal
- Separated non-durable in-memory stores into appendix (6 stores: TrustedManifest, Approval, EventQueue, SocketCatalogue, ScopeBindings, ExecutionEnvironment)
- Each store appears in exactly one class
- Corrected Trail classification: append-only JSONL with `sync_data()`, NOT write-then-rename
- Identified `m3_store.rs` (StoreRoot) as shared persistence layer for 7 stores
- Marked Installation Recovery Plan as read-only planner, not a store
- Routed UNVERIFIED file/directory durability claims to F3b
- Corrected write primitive descriptions: Trail does NOT use write-then-rename; Replay Ledger is the only store with directory-entry durability (dual `FlushFileBuffers`)

### Debt ledger correction

- Consolidated D2+M4 (missing .mli) into single M4 as Maintainability debt
- Reclassified D3 (dead `max_line_bytes`) to M5: Maintainability (no proven runtime harm)
- Reclassified D4 (dead `FailingResultAnchorWriter`) to M6: Maintainability (no proven runtime harm)
- Reclassified A2 (no OCaml tests) to M7: Maintainability (no contract violated)
- Reclassified A3 (test modules in src/) to M8: Maintainability (no contract violated)
- Added M9: test infrastructure `pub(crate)` visibility
- Retained D1 as Confirmed defect but explicitly qualified: the confirmed defect is nondeterministic test behaviour; production root cause unconfirmed
- Marked live-stderr as F2 candidate only (no F1 reproduction evidence)
- New counts: 1 Confirmed defect, 1 Contract ambiguity, 9 Maintainability, 2 Performance, 3 Documentation, 1 F2 candidate = 17 total (was: 4+3+5+2+3)

### Baseline transcript correction

- Described environment as existing clean worktree, not a fresh clone
- Recorded that timing was NOT CAPTURED for major commands (cold startup time not measured)
- Stated cold/warm distinction for test runs
- Noted OCaml engine binaries are pre-built artefacts from prior build, not rebuilt during F1
- Noted OCaml-level warnings were not captured

### Test inventory correction

- Stated inventory is representative (mapped to contracts), not exhaustive (not all 1,254 tests individually mapped)

### FIXTURE_MANIFEST correction

- Updated with per-fixture provenance: owner/contract, exact source command or source fixture/test, accepted baseline SHA, capture method, exit code where applicable, normalisation notes, review purpose
- Distinguishes binary captures from protocol fixture copies from test assertion constructions
- No fixture generation script exists or was added

## Evidence

Final serial verification matrix will be run after final artifact edit. Results to be recorded here.

## Discoveries (from first round, still valid)

1. Flaky test: `m3_windows_handle_allow_list_excludes_unrelated_inheritable_handle` — failed first run, passed subsequent. Likely timing-dependent Windows handle enumeration.
2. Fully JSON-enveloped CLI: even `--help` returns a JSON envelope with `exit_code: 2`, `status: invalid_cli_usage`.
3. Exit code convention: CLI uses exit code 2 for invalid usage, 0 for success (even when envelope reports errors in data).
4. OCaml engine has no direct tests: all engine testing is via Rust host integration or PowerShell scripts.
5. No `.mli` files: OCaml modules expose full internals with no abstraction boundaries.
6. `application.rs` at 8,260 lines: largest file, mixing concerns including dead code, test infrastructure, and runtime orchestration.

## New discoveries (correction round)

7. Replay Ledger is the ONLY store with directory-entry durability (dual `FlushFileBuffers`). All 7 StoreRoot-backed stores lack directory flush.
8. Trail uses append+sync in place, not write-then-rename. No per-line digest or integrity footer.
9. `m3_store.rs` (StoreRoot) is the common persistence layer for 7 stores, providing a consistent pattern but shared directory-durability gap.
10. Local Anchor Admission Store lacks `verify_chain()` on its store root — unlike all other durable stores.

## Remaining risks

- The flaky M3 test may indicate a genuine handle enumeration race on Windows but is not consistently reproducible.
- Directory durability not independently verified for any store except Replay Ledger; F3b is tasked with this.
- OCaml toolchain not available in this worktree; engine compilation and OCaml-level warnings were not captured.
- FileTrail has no path safety, no per-line integrity checks, and no corruption detection.

## Smallest next action

Run the complete required verification matrix serially at the actual HEAD after the F1 evidence checkpoint commit. If all mandatory commands are PASS, mark COMPLETE and push the branch.

## References

- Base: `24428139807cac0adeb0b62264547e61ca809d16` (`origin/main`)
- Prep checkpoint: `158422a54bede77ea59d6d08fe1fcdb5ed21d499`
- Task packet: `docs/CURRENT_CLINE_TASK.md`
- Architecture: `docs/architecture/TETHERS_FOUNDATION_PASS.md`
- F1 artifacts: `docs/foundation-pass/`
