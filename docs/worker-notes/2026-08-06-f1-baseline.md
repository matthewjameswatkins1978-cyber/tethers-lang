# Worker Note

Task: `F1 - Baseline and debt inventory`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `24428139807cac0adeb0b62264547e61ca809d16`

Implementation checkpoint: `fe6609688a4dbbef996528c1077d039e02dba486`

## Requested outcome

Produce a documentation-and-evidence-only baseline for the Foundation Pass: validate the provisional pre-pass baseline, record measured repository facts, create independent literal compatibility fixtures, and classify all discovered debt without repairing it.

## Changes made

1. **Packet correction** (`a2ea5ce`): Fixed `Base commit` from `4262cf5e...` to `24428139...` (the accepted main baseline), added `Preparation checkpoint: 158422a5...`. Set status to `IN_PROGRESS`.

2. **F1 evidence checkpoint** (`fe66096`): Created `docs/foundation-pass/` with:
   - `BASELINE_TRANSCRIPT.md` — clean-checkout environment, Git facts, toolchain, build results
   - `MODULE_DEPENDENCY_MAP.md` — Rust host (53 modules, ~54K lines) and OCaml engine (6 modules, 924 lines) with dependency graph
   - `TEST_INVENTORY.md` — 1254 unit + 250 integration tests mapped to J13-J24 contracts
   - `WARNING_INVENTORY.md` — 137 Clippy warning occurrences categorized by file and type
   - `PERSISTENCE_INVENTORY.md` — Classified stores: immutable atomic records, replaceable current-state records, append-only causal logs, multi-step intent/recovery journals, unclassified/transient
   - `DEBT_LEDGER.md` — 17 items: 4 confirmed defects, 3 contract ambiguities, 5 maintainability debts, 2 performance hypotheses, 3 documentation debts. All assigned to F-packages or deferred.
   - `FIXTURE_MANIFEST.md` — Manifest of all literal fixtures with declared source capture method
   - `fixtures/` — 13 literal committed artifacts: CLI help/output, exit cases, JSON envelopes, trail record shape, replay digest shape, installation outcomes, recovery states

No production code, test code, Cargo.lock, or dependency changes.

## Decisions and assumptions

- The `m3_windows_handle_allow_list` test is flaky (failed 1st run, passed 2nd run). Recorded as Confirmed defect D1 without attempting repair.
- The CLI output is fully JSON-enveloped; even `--help` produces a JSON envelope with `exit_code: 2` and `status: invalid_cli_usage`. Fixtures were captured from warm binary at the baseline commit.
- OCaml engine was not built during this baseline — the Rust toolchain sufficed for the required verification matrix (packet says OCaml switch path is N/A).
- Literal fixtures were manually captured rather than programmatically generated, satisfying the independence requirement.

## Evidence

Final serial verification matrix (all commands run after final F1 artifact edit `fe66096`):

| # | Command | Result |
|---|---|---|
| 1 | `git fetch origin --prune` | PASS |
| 2 | `git rev-parse origin/main` | PASS (`24428139807cac0adeb0b62264547e61ca809d16`) |
| 3 | `git rev-parse HEAD` | PASS (`fe6609688a4dbbef996528c1077d039e02dba486`) |
| 4 | `git status --short --branch` | PASS (clean, ahead 2) |
| 5 | `rustup show` | PASS (1.97.1 active) |
| 6 | `cargo --version` | PASS (1.97.1) |
| 7 | `cargo fmt --all -- --check` | PASS (no violations) |
| 8 | `cargo check --all-targets --all-features --locked` | PASS (compiles) |
| 9 | `cargo test --all-targets --all-features --locked` | PASS (1254 passed, 0 failed warm) |
| 10 | `cargo clippy --all-targets --all-features --locked -- -W clippy::all` | PASS (137 warnings, 0 errors) |
| 11 | `just verify` | PASS (packet checker + fmt + check + tests) |
| 12 | `just verify-agent` | PASS (1504 passed, 2 skipped) |
| 13 | `pwsh -NoProfile -File .github/scripts/check-tethers-task-packet.ps1` | PASS |
| 14 | `git diff --check origin/main...HEAD` | PASS (no whitespace errors) |
| 15 | `git diff --name-only origin/main...HEAD` | PASS (23 files, all docs) |
| 16 | `git status --short --branch` | PASS (clean, ahead 2) |

Test totals: 1254 unit + 250 integration = 1504 tests. 2 skipped.
Warning totals: 137 Clippy warning occurrences. 0 compile errors.
Confirmed defects: 4 (D1: flaky M3 handle test, D2: missing .mli interfaces, D3: dead `max_line_bytes` field, D4: dead `FailingResultAnchorWriter`).

## Discoveries

1. **Flaky test**: `m3_windows_handle_allow_list_excludes_unrelated_inheritable_handle` failed first run, passed second. Likely timing-dependent Windows handle enumeration.
2. **Fully JSON-enveloped CLI**: Even `--help` returns a JSON envelope rather than raw text.
3. **Exit code convention**: CLI uses exit code 2 for invalid usage, 0 for success (even when envelope reports errors).
4. **OCaml engine has no direct tests**: All engine testing is via Rust host integration or PowerShell scripts.
5. **No `.mli` files**: OCaml modules expose full internals with no abstraction boundaries.
6. **`application.rs` at 8,260 lines**: Largest file, mixing concerns including dead code, test infrastructure, and runtime orchestration.

## Remaining risks

- The flaky M3 test may indicate a genuine handle enumeration race on Windows but is not consistently reproducible.
- Directory durability not independently verified; F3b is tasked with this.
- OCaml toolchain not available in this worktree; engine compilation and OCaml-level warnings were not captured.

## Smallest next action

Await Lucy's independent review of the `foundation/f1-baseline` branch. If accepted, Lucy compiles F2 (operational correctness defects) based on F1 findings.

## References

- Base: `24428139807cac0adeb0b62264547e61ca809d16` (`origin/main`)
- Branch tip: `fe6609688a4dbbef996528c1077d039e02dba486`
- Prep checkpoint: `158422a54bede77ea59d6d08fe1fcdb5ed21d499`
- Task packet: `docs/CURRENT_CLINE_TASK.md`
- Architecture: `docs/architecture/TETHERS_FOUNDATION_PASS.md`
- F1 artifacts: `docs/foundation-pass/`
