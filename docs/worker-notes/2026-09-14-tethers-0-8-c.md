# Tethers 0.8-C / R3 worker note

Task: `Tethers 0.8-C / R3`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Codex`

Status: `BLOCKED`

Base commit: `ddc5d0fccfc0a38ff0af3013c5e9ff6ba357e0b2`

Implementation checkpoint: `WORKTREE`

## Requested outcome

Implement the authorised native GNU/Linux x86-64 parity slice while preserving Windows semantics, and leave support claims provisional until the native CI and clean-package gates are accepted.

## Changes made

Added a Linux replay backend and shared replay-runtime selection; ported Unix installation locking, Linux process-group/PDEATHSIG supervision, and Linux launch environments; made engine discovery, verification reports, compatibility paths, and MCP transcript execution platform-aware; added Linux package and clean-extraction smoke scripts; added the Ubuntu CI lane; and updated the packet, audit, security, verification, roadmap, README, and quickstart documentation.

## Decisions and assumptions

Linux is `x86_64-unknown-linux-gnu` with glibc, not musl or WSL. Linux provider cleanup guarantees the direct provider and ordinary descendants that remain in its process group; a provider that deliberately creates a new session is outside the documented non-sandbox guarantee. The Linux package is a candidate artifact only and does not claim final R5 SBOM, attestation, or package-manager support.

## Evidence

The repository startup gate passed: fetched `origin/main` matched base `ddc5d0f`; the isolated branch is `codex/tethers-0.8-linux-native-parity`. `scripts/check-dev-tools.ps1` passed. Windows `just fmt` and `just check` passed. Windows compatibility corpus, fixture sanity, and packet checker passed. Ubuntu WSL pre-`O_NOFOLLOW` focused Linux tests passed: replay lifecycle/symlink 2/2, Unix supervision 2/2, installation locks 9/9. Ubuntu WSL `cargo check --all-targets --all-features --locked` with `RUSTFLAGS=-D warnings` passed before the final `O_NOFOLLOW` hardening edit. The Windows direct library run reported 1,554 passed, 22 engine-prerequisite failures, 2 ignored; the failures were the expected missing-provenance-engine cases. `just verify` passes packet/format, then fails current-engine preparation and records dependent suites as skipped with reason.

## Discoveries

The checkout has no repository-local OCaml switch, native Linux target, or current OCaml engine available to this run. WSL later failed to start with Windows service error `0x800705aa` (insufficient system resources), so the post-hardening Linux compile could not be rerun. GitHub-hosted Ubuntu CI is therefore required to establish native Core, cross-language, package, and clean-install evidence.

## Remaining risks

Native Linux compilation after the final `O_NOFOLLOW` edit is unverified in this host, native OCaml/Core and MCP suites are unverified, the Ubuntu CI job has not run, and no Linux tarball was produced locally. Linux must remain NOT YET OFFICIAL and the branch must not be reported release-eligible.

## Smallest next action

Run the branch’s Ubuntu CI job with OCaml 5.5.0, Dune 3.24.0, Yojson 2.2.2, and Digestif 1.3.1; inspect native `just verify`, package, checksum, and clean-install smoke results; then resolve any failures before changing the packet to COMPLETE.

## References

`docs/LINUX_PORTABILITY_AUDIT.md`; `scripts/package-linux.ps1`; `scripts/test-linux-package.ps1`; `.github/workflows/tethers-verification.yml`; `verification/tethers-verification.json` (ignored local report); Rust focused test output from the Ubuntu WSL checkout.
