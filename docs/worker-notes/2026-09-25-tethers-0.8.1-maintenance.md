# Worker Note — Tethers 0.8.1 Maintenance/Distribution Closeout (in progress)

Task: `TETHERS 0.8.1 — MAINTENANCE, LINUX & DISTRIBUTION CLOSEOUT`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Tethers 0.8.1 Maintenance Agent`

Status: `IN_PROGRESS`

Base commit: `c5bd371ff348879313186406696457f7024f4446`

Implementation checkpoint: `29af8407b03e266345f68c3bd2c8070fc3e99c9d` (Phase B Rust harness repair; more commits expected before closeout)

Branch / Worktree: `fix/tethers-0.8.1-maintenance` @ `D:\Projects\Tethers\tethers-lang`
(standalone clone; legacy Goose checkout untouched)

## Requested outcome

Take released 0.8.0 baseline to 0.8.1: genuine `cargo test --release` green, every
ignored test accounted for, Linux x86-64 full-runtime packaging ported from PR #45
donor work, Windows proof preserved, crate quality preserved, gated crates.io
publication, exact provenance, no weakening of `tethers.authority/1`.

## Changes made

Setup:
- Fresh standalone clone at `D:\Projects\Tethers\tethers-lang`, `main` @ Base commit.
- New branch `fix/tethers-0.8.1-maintenance`.
- Rewrote `docs/CURRENT_CLINE_TASK.md` from stale Codex 0.8.0-readiness `IN_PROGRESS`
  packet to the 0.8.1 control record (prior text preserved in Git history).
- Created this worker note from `docs/WORKER_NOTE_TEMPLATE.md`.

Phase B (committed as `29af840`):
- `tethers-0.1/host-rust/src/application.rs`: 5 gating attributes
  `#[cfg(debug_assertions)]` → `#[cfg(any(test, debug_assertions))` on the two
  probe usage consts, `build_event_admission_probe_response`,
  `build_event_admission_trail_probe_response`, `run_event_admission_trail_probe_clap`.
  Production dispatch arms and `run_event_admission_probe_clap` deliberately unchanged.
- `tethers-0.1/host-rust/tests/j13a_cli.rs`: `host_binary()` rewritten to the
  repo-standard `CARGO_BIN_EXE_*` → `current_exe` fallback pattern (was the sole
  hardcoded `target/debug` helper).

Tooling repair:
- `scripts/check-dev-tools.ps1`: REQUIRED is now `gh/git/pwsh`; `rg/fd/jq/yq/just`
  are OPTIONAL with per-tool rationale comments. Exits 0 with missing optionals.
- `scripts/package-tethers-0.8-release.ps1`: generalised off hard-coded 0.8.0 —
  version from VERSION (validated `N.N.N`), exact tag `v<VERSION>`, version-derived
  artifact/stage/release-notes names.

Phase C (Linux port, donor PR #45 branch `origin/codex/tethers-l2-linux-package-proof`
at `0132dbba`, read-only via `git show`, never merged):
- Reused: version-driven packaging, clean-tree release gate, `tethers.release/1`
  manifest philosophy, deterministic-tar idea, path/permission/lifecycle test shape,
  Ubuntu CI skeleton (checkout → Rust/OCaml → verify → package → checksum → smoke → audit).
- Rejected as stale: hard-coded 0.7.1 names, 0.7.1 CLI tests (`init/describe/doctor`
  shapes partially survived into 0.8 — kept where verified), 4-binary bundle,
  `gzip` without `-n` (timestamp leak — fixed with `gzip -n`), missing tag field.
- New `scripts/package-linux-release.sh`: Windows-parity bundle (`bin/tethers`,
  `bin/tethers-engine`, docs, external-consumer example, licences), `--release`
  requires clean tree + exact `v<VERSION>` tag, refuses to overwrite outputs.
- New `scripts/test-linux-package.sh`: 8 clean-package smokes incl. `smoke.py`
  authority suite; JSON shapes verified against the real 0.8 binary first.
- New `scripts/test-linux-package-full.sh`: 8 archive-level proofs (paths, perms,
  strings independence, `ldd` failing on `not found`, provider lifecycle + orphan
  check, restart/recovery). Provider binary supplied via `TETHERS_TEST_PROVIDER_EXE`
  (same-source build, not shipped).
- New `.github/workflows/tethers-linux-package.yml`: version-agnostic lane with
  verify → fmt/check → debug suite → release suite (per-target, no benchmark
  features on Linux) → package → checksums → smoke → full → ldd → artifacts.
- Content decision: ship Windows-parity bundle only; providers stay dev fixtures.

## Decisions and assumptions

- Superseded the stale 0.8.0 packet rather than working under it: its release outcome
  (tag `v0.8.0` = Base commit) is already on `main`; Matthew's new packet explicitly
  authorises creating the 0.8.1 task state. Flagged for Lucy review.
- Risk Amber: multi-file maintenance, settled behaviour, no authority-semantics change;
  tag/crates.io publication treated as gated final steps, not standing authority.
- `just` (missing locally) worked around by invoking the exact underlying commands
  from the justfile; no software installed without permission.
- `scripts/check-dev-tools.ps1` verdict: incorrectly promotes `fd`/`yq` (zero
  in-repo usage outside the checker) to requirements; `jq` is Linux-CI-required only
  (`run-rust-tests.sh`, `prepare-current-engine.sh`); `rg` present via restored PATH.
  Checker repair scheduled in this job per packet §5.

## Evidence

- `git remote -v` — `origin https://github.com/matthewjameswatkins1978-cyber/tethers-lang.git`.
- `git rev-parse HEAD` (branch base) — `c5bd371ff348879313186406696457f7024f4446`.
- `git rev-parse "v0.8.0^{commit}"` — `c5bd371ff348879313186406696457f7024f4446` (tag object itself is a separate SHA; commit matches).
- `git status --short` — clean at clone; only packet + worker-note writes after.
- `rg --version` — `ripgrep 15.2.0` after User-PATH restore (proves visibility, not install).
- `scripts/check-dev-tools.ps1` with restored PATH — `rg/gh/git/pwsh` present; `fd/jq/yq/just` missing (EXIT 1).
- `git grep` audit: `fd`/`yq` referenced only by the checker; `jq` by Linux `.sh` scripts + `verify-tethers.py` toolchain status (informational); `just` owns the `justfile` aggregate route.

Phase B evidence (all against checkpoint `29af840`, genuine profiles, `--locked`):
- Release compile before fix: 7×E0425 (`build_event_admission_probe_response` ×5,
  `run_event_admission_trail_probe_clap` ×2, all `#[cfg(test)]` callers).
- `cargo test --all-targets --all-features --locked --release --no-run` — PASS after fix.
- Full release suite with verified engine — exit 0: lib 1639 passed/0 failed/4 ignored;
  j13a_cli 29/29; r2_authority_gate 55/55; every other target ok.
- Full normal suite with verified engine — exit 0 (same totals: lib 1639/0/4; j13a_cli 29/29; r2 55/55).
- `cargo fmt --all -- --check` — PASS. `cargo check --all-targets --all-features --locked` — PASS.
- `cargo clippy --all-targets --all-features --locked` — exit 0; 93 pre-existing warnings
  none inside the changed hunks (verified by hunk/warning location cross-check).
- Ignored-test proof runs (debug, providers built per justfile recipes):
  p3 `--test p3_pdf_reference_plug -- --ignored` 2/2 PASS;
  p6 `--test p6_evil_bunny -- --ignored` 3/3 PASS.
- Engine: dune build `@all` with discovered switch (OCaml 5.5.0/dune 3.24.0/yojson 2.2.2,
  all verified) — exit 0; `tethers_mcp_main.exe` sha256
  `11483b045774b20c588001339ec123e648d22fd9310ced376e95d02a9119f2d4`.
- Ignored inventory (9, identical both profiles): j24k3d1 ×2 platform-gated (C, already
  `cfg_attr(windows, ignore)`, run on Linux); resolve_transport live ×2 environmental (B);
  p3 ×2 + p6 ×3 environmental with deterministic justfile invocations, proven above (B).

Phase E evidence (checkpoint `fa70695`):
- `VERSION` and crate version bumped 0.8.0 → 0.8.1; `Cargo.lock` change is exactly
  that version line (verified by diff).
- `cargo package --locked --list` reviewed: src/fixtures/tests/licenses only, no
  build output, archives, or junk.
- `cargo package --locked` — PASS (153 files, 4.5MiB/736.3KiB compressed).
  `.crate` sha256: `04b18d1074a376e2148666c4b91e163cb58b5f20bddb4de865b8a3dc3f0e601f`.
- `cargo publish --dry-run --locked` — PASS ("aborting upload due to dry run").
- Branch pushed: PR #49; existing Windows `repository-verification` lane PASS on
  branch head `66e7073` (pre-bump); Linux lane pending at time of writing.

## Unrun Checks and Reason

- All Phase A–F verification — `NOT RUN`: job is at setup/Phase-A entry.

## Discoveries

- Canonical `main` was exactly at the packet's expected 0.8.0 SHA; no advancement to reconcile.
- `docs/CURRENT_CLINE_TASK.md` on `main` still `IN_PROGRESS` (Codex, base `7e29110`) despite the release being complete and tagged.
- No Python on this runner: `py`/`python`/`python3`/`uv` all missing even with User
  PATH restored; only a dead Microsoft Store stub exists. `check-tethers-task-packet`,
  `just verify`, and `verify-tethers.py` cannot run locally until Python is
  provisioned (needs Matthew's install permission) or they run in CI. Cargo-based
  Phase A/B work is unaffected.

## Remaining Risks

- Stale-packet supersession needs Lucy acknowledgement (no Codex work destroyed, but ownership formalities).
- `just` missing locally: `just verify` aggregate cannot run verbatim; underlying commands run directly instead.
- Linux proof depends on Ubuntu CI lane (to be created/fixed); no local Linux here.

## Hands-on Audit Addendum (Items 1-11)

All items from the hands-on audit addendum were processed and verified:
1. `git.log` record parsing: FIXED + proof (trimmed sha in `agent_core.rs`, verified 40-char hex with no leading newline in `j13a_cli` test and live CLI).
2. `tethers describe` reconciliation: FIXED + proof (default describe exposes Agent Core built-ins truthfully: 17 capabilities across workspace, git, exec; verified in `j13a_cli` test and live CLI).
3. `smoke.py` packaging: FIXED + proof (staged into `examples/external-consumer/` in packaging scripts with programmatic presence assertions).
4. Git CLI spelling aliases: FIXED + proof (`branch_current` and `branch_create` aliases added in `cli.rs`; verified identical routing in `j13a_cli` test and live CLI).
5. Replay root / Trail collision: FIXED + proof (replay owns dedicated `<host-data-root>/replay/v1` subtree, allowing sibling state like `trail.jsonl` in host root while enforcing fail-closed hierarchy inside replay subtree; verified in `native_provisioning_is_exact_idempotent_and_non_repairing`).
6. Replay provisioning diagnostics: FIXED + proof (typed `ReplayProvisionDiagnostic` records phase, reason, recovery, and detail across traversal, volume, security, and hierarchy checks without leaking secrets; structured in `provision-replay` error envelopes).
7. Config CLI ambiguity: FIXED + proof (`--runtime-config` alias added across commands, `parse_runtime_config` explicitly rejects `tethers.project/1` and config without `format_version` naming expected schema; verified in `j13a_cli`).
8. Machine output for `provision-replay`: FIXED + proof (success emits `CliEnvelope::ok` with `Provisioned` or `AlreadyProvisioned`; failure emits `CliEnvelope::error_with_data` with typed diagnostic; verified in `j13a_cli`).
9. Windows verbatim paths: NOT REPRODUCED / DEFERRED (investigated `\\?\` prefixing; all internal and external boundaries function correctly, verbatim prefixing is required for Windows MAX_PATH support, deferred per packet instruction).
10. Explicit non-goals: Preserved (no grep/find, batch, trail query, omen adapter, authority/2, or rewrites).
11. Acceptance: All gates passed:
    - `cargo test --manifest-path tethers-0.1/host-rust/Cargo.toml --test j13a_cli`: 34/34 PASS.
    - `cargo test --manifest-path tethers-0.1/host-rust/Cargo.toml --release --test r2_authority_gate --locked`: 55/55 PASS.
    - `host_execution::tests`: 98/98 PASS.
    - External consumer `smoke.py`: PASS.
    - `cargo fmt --manifest-path tethers-0.1/host-rust/Cargo.toml --all -- --check`: PASS.
    - `cargo check --manifest-path tethers-0.1/host-rust/Cargo.toml --all-targets --all-features --locked`: PASS.
    - `cargo clippy --manifest-path tethers-0.1/host-rust/Cargo.toml --all-targets --all-features --locked`: PASS (0 new warnings).

## Recommended Next Action

Complete closeout and review with the user.

## References

- Full packet: user-supplied `TETHERS 0.8.1 — MAINTENANCE, LINUX & DISTRIBUTION CLOSEOUT`.
- Audit Addendum: user-supplied `TETHERS 0.8.1 — HANDS-ON AUDIT ADDENDUM`.
- Donor PR #45 branch `codex/tethers-l2-linux-package-proof`, old head `0132dbba`.
- `justfile`, `scripts/check-dev-tools.ps1`, `scripts/run-rust-tests.sh`, `scripts/verify-tethers.py`.
