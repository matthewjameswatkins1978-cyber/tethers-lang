# Worker Note

Task: `TETHERS-0.3-P1-R1G-FIX — Correct J23C3 Scope Digest Assertion`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `a0390fbfb5df439b3c000c39537d16e6ce198e7f`

Implementation checkpoint: `83b419c5275dd6edc773eacea16dabbe4c286f7c`

## Requested outcome

Correct one stale J23C3 test assertion exposed by the R1G final verification gate. The test at `j23c3_installed_pdf_execution.rs:226` used `assert_eq!` when two scopes have different content (different `max_bytes` values), but `OperationalScopeEvidence` integrity digest includes scope content by design, so the assertion must be `assert_ne!`.

## Changes made

`tethers-0.1/host-rust/tests/j23c3_installed_pdf_execution.rs`:
- Line 226: `assert_eq!` → `assert_ne!` (scope content differs → digest must differ)
- Line 242: "enablement scope does not match supplied scope" → "enablement pins are stale" (the pin check at `launch_profile.rs:241` catches digest mismatch before content comparison at `launch_profile.rs:249`)

No production files changed.

## Decisions and assumptions

1. The second error-message correction was necessary because the digest mismatch is caught by the pin check (line 241) before reaching the content comparison (line 249). Both are valid rejection paths.
2. The rejection proof remains intact — mismatched scope is still refused, just through the digest-mismatch path rather than the content-mismatch path.
3. No production, digest, enablement, or launch behaviour changes were made.

## Evidence

### Test result

```
cargo test --locked --test j23c3_installed_pdf_execution
running 1 test
test installed_pdf_plug_lifecycle ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```

### Format and whitespace

```
cargo fmt --all -- --check
(no output — clean)
```

```
git diff --check
(only LF/CRLF checkout warnings, no whitespace errors)
```

### Diff gate

```diff
-    assert_eq!(mismatched_scope.integrity_digest, scope.integrity_digest);
+    assert_ne!(mismatched_scope.integrity_digest, scope.integrity_digest);
...
-    assert!(mismatched_error.contains("enablement scope does not match supplied scope"));
+    assert!(mismatched_error.contains("enablement pins are stale"));
```

Exactly 2 lines changed in the test file. No production files in diff.

### Task packet checker

```
control-v1/COMPLETE
```

## Publication evidence

- Branch: `feature/0.3-p1-r1g-fix-j23c3-scope-digest`
- Remote: pushed to `origin`
- Remote HEAD resolved, local == remote confirmed
- Final `git status --short --branch` clean

## Discoveries

1. The J23C3 test had a second defect beyond the assertion: the expected error message was also stale. The digest check at `launch_profile.rs:241` ("enablement pins are stale") fires before the content check at line 249 ("enablement scope does not match supplied scope"), so when scope content changes, the pin check catches the digest difference first.
2. The R1G BLOCKED task's untracked worker note (`2026-08-10-0.3-p1-r1g-final-gate.md`) remains in the working tree. It is from the prior BLOCKED task and was intentionally not committed here.

## Remaining risks

None known within packet scope.

## Smallest next action

Rerun R1G final P1 verification from the accepted R1G-FIX head (`83b419c5275dd6edc773eacea16dabbe4c286f7c`).

## References

- `src/operational_scope.rs:66-81` — integrity digest includes canonical_scope_json
- `src/launch_profile.rs:237-254` — pin check before content comparison
- `tests/j23c3_installed_pdf_execution.rs:226` — corrected assertion
- `tests/j23c3_installed_pdf_execution.rs:242` — corrected error message
