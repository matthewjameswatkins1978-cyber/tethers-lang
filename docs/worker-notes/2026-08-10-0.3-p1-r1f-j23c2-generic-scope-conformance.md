# Worker Note

Task: `TETHERS-0.3-P1-R1F — Migrate J23C2 to Generic Scope Conformance`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `OpenCode`

Status: `COMPLETE`

Base commit: `36f1790550309b5695cfd1aa4f3e03223594dba8`

Implementation checkpoint: `08d0b81bca2fae8d1da5bfeb1d654c78db421a6f`

## Requested outcome

Migrate the stale J23C2 PDF conformance test from the retired `--query-root` / `PDF_QUERY_ROOT_PLACEHOLDER` placeholder model to the current generic Operational Scope architecture. Prove the provider starts under normal scope delivery, conformance fallback only on exact `TETHERS_CONFORMANCE=1`, and the real package passes host conformance with empty launch arguments. No production code changes.

## Changes made

- `tethers-0.1/host-rust/tests/j23c2_pdf_conformance.rs` — complete rewrite of Part A and targeted update of Part B:
  - Removed `PDF_QUERY_ROOT_PLACEHOLDER` constant and three stale placeholder tests (`placeholder_without_conformance_is_refused`, `placeholder_with_conformance_zero_is_refused`, `unknown_pdf_placeholder_is_refused`).
  - Added `normal_mode_without_operational_scope_refuses` and `conformance_zero_without_scope_refuses` tests proving normal-mode and non-"1" conformance refusal.
  - Updated `normal_scope_with_valid_json_starts` to use `normal_session()` with explicit `max_bytes` (not the old `spawn_session` implicit hard-coded bound).
  - Added `conformance_session()` helper setting only `TETHERS_CONFORMANCE=1` and `TEMP`; replaced the old placeholder-based conformance startup test.
  - Updated `conformance_with_missing_temp_refuses` and `conformance_with_invalid_temp_refuses` to remove `--query-root` CLI argument.
  - Part B assertions changed: `launch_arguments` asserted empty (was placeholder), added environment-name checks for `TETHERS_CONFORMANCE`/`TEMP` presence and `TETHERS_OPERATIONAL_SCOPE_JSON`/`TETHERS_OPERATIONAL_SCOPE_DIGEST` absence.
  - Retained all existing case assertions, frozen manifest digest, and no-conformance_session-failure proof.

No production files changed. No other test files, packages, or configuration changed.

## Decisions and assumptions

- `normal_session()` passes a distinct `max_bytes=1048576` (not `MAX_PDF_BYTES`) to prove the exact value from JSON is used, unlike the old hard-coded default.
- Environment-name assertions use exact case-sensitive string comparison against the BTreeMap keys in `approved_environment()`.
- The `env_remove` calls for refusal tests explicitly remove `TETHERS_OPERATIONAL_SCOPE_JSON` to guarantee the provider cannot find leftover env vars from the build/test harness.

## Evidence

All commands run from `tethers-0.1/host-rust`:

| Command | Result |
|---|---|
| `cargo test --locked --test j23c2_pdf_conformance` | 7 passed, 0 failed (0.75s) |
| `cargo test --locked --bin pdf_tools_provider` | 9 passed, 0 failed |
| `cargo test --locked --test j23b_pdf_package` | 1 passed, 0 failed (0.63s) |
| `cargo check --all-targets --all-features --locked` | clean |
| `cargo fmt --all -- --check` | clean (after rustfmt applied) |
| `git diff --check` | clean (CRLF checkout warning only) |
| `rg "__TETHERS_PDF_QUERY_ROOT__\|PDF_QUERY_ROOT_PLACEHOLDER\|--query-root" tests/j23c2_pdf_conformance.rs` | no matches |

## Publication evidence

Branch: `feature/0.3-p1-r1f-j23c2-generic-scope-conformance`

(Push pending — will be recorded after normal push.)

## Discoveries

None. The current generic scope architecture worked without any production change needed.

## Remaining risks

None known within packet scope. The retired placeholder names are completely absent from the J23C2 file as confirmed by the rg check. No production files reference them either (they were never in production code of this branch's base).

## Smallest next action

Lucy reviews the pushed diff, worker note, and test evidence to accept or reject. The remaining P1 work continues from the next compiled task.

## References

- Base: `36f1790550309b5695cfd1aa4f3e03223594dba8` on `feature/0.3-p1-r1e-synthetic-unrelated-plug`
- Implementation: `08d0b81bca2fae8d1da5bfeb1d654c78db421a6f` on `feature/0.3-p1-r1f-j23c2-generic-scope-conformance`
- `tethers-0.1/host-rust/src/bin/pdf_tools_provider.rs` — provider startup/scope resolution (read, not changed)
- `tethers-0.1/host-rust/src/pdf_tools.rs` — `build_reference_package()` with empty `launch.arguments` (read, not changed)
- `tethers-0.1/host-rust/src/launch_profile.rs` — `approved_environment()` and `PreparedSupervisedLaunch::prepare()` (read, not changed)
- `tethers-0.1/host-rust/src/conformance.rs` — `run_host_conformance()` (read, not changed)
