# Worker Note

Task: `Tethers 0.8-B / R2`

Task packet: `docs/CURRENT_CLINE_TASK.md`

Owner: `Codex`

Status: `COMPLETE`

Base commit: `9b8b1fe86551705626785e6ed76a784f91fe6515`

Implementation checkpoint: `668648cdb3a248b803ac1cc7f04b65028de52fd8`

Final Commit: `PENDING CLOSEOUT COMMIT`

Branch / Worktree: `codex/tethers-0.8-verification-release-control` / `D:\The Next Thing\Tethers Lang - J16 Clean`

## Requested outcome

Make repository-owned verification authoritative about source, toolchains,
current OCaml engine provenance, required suites, compatibility evidence, and
release eligibility.

## Changes made

- Historical completed packet checkpoints validate their accepted commit without treating later repository work as stale evidence.
- Active scoped evidence can detect implementation changes after its evidence checkpoint.
- Current engine preparation builds `@all`, records `tethers.engine/1` source/tree/binary identity, and stops before Rust when prerequisites fail.
- Rust cross-language test helpers require the explicitly verified engine; no stale `_build`, PATH, or sibling-worktree fallback remains.
- `just verify` now runs the aggregate route and writes `tethers.verify/1` machine evidence.
- Added required/optional/external outcome documentation, warning ratchet, 0.7 compatibility seed corpus, and bounded Windows PR workflow.

## Decisions and assumptions

No product, language, Plan, Trail, policy, Plug, or MCP semantics changed.
Apache-2.0 remains an owner decision. Linux runtime and release parity remain
R3/R5 requirements.

## Evidence

- Reproduced the prior failure with `cargo test --manifest-path tethers-0.1/host-rust/Cargo.toml core9b_t5_evaluate_is_core -- --nocapture`: stale repository-relative engine returned `Unknown Capability: notify`.
- Fresh current-source build initially failed on missing `digestif`; the declared locked dependency was installed in the existing path-bound switch, then `dune build @all` succeeded.
- Focused Core-9B test passed against the rebuilt engine.
- `just verify` passed after implementation: packet, formatting, current engine, OCaml tests, Rust static checks, warning ratchet, 1,576 Rust library tests plus targets/cross-language tests, protocol fixtures, 16 MCP transcripts, and compatibility corpus.
- Packet checker tests: 9 passed, 0 failed.
- Toolchain diagnostic passed with Rust 1.97.1, OCaml 5.5.0, Dune 3.24.0, Yojson 2.2.2, and Digestif 1.3.1.

## Discoveries

The 13 reported Rust failures shared one root cause: an old engine was selected
without provenance, while the current switch lacked Digestif and could not
build the source. C-B1 remains obsolete benchmark residue, not a current
regression. The GARY post-checkpoint complaint is external task-checker
compatibility, not Tethers product correctness.

## Remaining risks

The bounded CI workflow proves repository/Rust/fixture foundations on Windows;
it does not claim Linux runtime support or replace the local OCaml-switch route.
The existing duplicate Cargo target warning remains accepted debt.

## Smallest next action

Push, review, and merge this branch; begin R3 Linux Native Parity only from the
accepted resulting `origin/main` SHA.

## References

- `docs/VERIFICATION.md`
- `docs/TETHERS_1_0_PREFLIGHT.md`
- `verification/tethers-verification.json` (generated, ignored)
