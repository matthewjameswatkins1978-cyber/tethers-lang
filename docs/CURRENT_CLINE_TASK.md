# Current Implementation Task

Control contract: `1`
Task: `F1 - Baseline and debt inventory`
Owner: `OpenCode`
Model: `OpenCode`
Status: `COMPLETE`
Task colour: `Amber`
Route: `OpenCode performs one documentation-and-evidence-only baseline task; Lucy independently reviews the resulting branch`
Worker note: `docs/worker-notes/2026-08-06-f1-baseline.md`
Base branch: `main`
Base commit: `24428139807cac0adeb0b62264547e61ca809d16`
Implementation branch: `foundation/f1-baseline`
Parent branch: `main`
Parent tip: `24428139807cac0adeb0b62264547e61ca809d16`
Preparation checkpoint: `158422a54bede77ea59d6d08fe1fcdb5ed21d499`
OCaml switch path: `N/A`
Rust toolchain: read exact channel from `rust-toolchain.toml`; use plain Cargo (resolved by root pin); `--locked` mandatory
Toolchain preflight: `pwsh -NoProfile -File scripts/check-dev-tools.ps1`

## Objective

Produce a reproducible, documentation-and-evidence-only baseline for the
Foundation Pass. Revalidate the provisional pre-pass baseline, record measured
repository facts, establish independent literal compatibility fixtures, and
classify all discovered debt without repairing it.

## Relevant background and existing behaviour

J24K and J24L are accepted. `24428139807cac0adeb0b62264547e61ca809d16` was
revalidated as `origin/main` on 2026-08-06 and is the provisional pre-pass
baseline. The governing programme plan is
`docs/architecture/TETHERS_FOUNDATION_PASS.md`; the J24L lessons are in
`docs/working-guides/DEEPSEEK_PRO_OPENCODE_JOB_PLAYBOOK.md`.

F1 measures the working repository before broader cleanup. It does not inherit
the live-stderr description as a defect: that candidate becomes an F2 task only
if F1 records a direct baseline characterisation.

## Required behaviour

1. Revalidate `origin/main` and the provisional baseline directly from Git,
   record the clean-checkout environment and serial command results, and report
   every required command as PASS, FAIL, or NOT RUN.
2. Create a module/dependency map, contract-mapped test inventory, warning
   inventory, and persistence inventory that distinguish immutable atomic
   records, replaceable current-state records, append-only causal logs, and
   multi-step intent/recovery journals.
3. Add a hand-reviewed literal compatibility fixture set covering the named
   public boundaries: CLI help and representative output, exit cases, JSON
   envelopes, Trail records, replay digests, installation outcomes, and recovery
   states. Fixtures must be independent of and never regenerated by the
   implementation under test.
4. Create a Foundation debt ledger. Every item must cite live evidence and one
   disposition: Confirmed defect, Contract ambiguity, Maintainability debt,
   Performance hypothesis, or Documentation debt. Assign an F-package or an
   explicit defer decision; do not fix any item.
5. Keep all work as documentation, evidence records, and literal fixtures. The
   final checkpoint must be documentation-only and must not alter production or
   test code.

## Relevant components

- `docs/architecture/TETHERS_FOUNDATION_PASS.md`
- `docs/working-guides/DEEPSEEK_PRO_OPENCODE_JOB_PLAYBOOK.md`
- `docs/foundation-pass/` (new baseline records and literal fixtures)
- `docs/worker-notes/2026-08-06-f1-baseline.md` (new)
- `docs/CURRENT_CLINE_TASK.md` (status and evidence update only)
- `tethers-0.1/host-rust/` and `tethers-0.1/engine-ocaml/` (read-only inventory)

## Frozen decisions and invariants

- The provisional pre-pass baseline is `24428139807cac0adeb0b62264547e61ca809d16`.
  If live `origin/main` differs, record the direct Git evidence and stop for a
  packet correction; do not substitute another base.
- Test access never justifies widening production visibility or adding a public
  test seam.
- The packet is authoritative. Do not weaken it to fit findings or an intended
  implementation; stop on a genuine contradiction.
- Tests and fixtures prove named properties directly. Generated output from the
  implementation under test is not an independent compatibility fixture.
- Capture all SHA values directly from Git commands; never expand a short SHA
  from memory.
- Each mandatory verification command must have one PASS, FAIL, or NOT RUN
  result. Any mandatory NOT RUN blocks COMPLETE.
- After the final F1 artifact edit, rerun the complete required matrix serially
  before claiming COMPLETE.
- Documentation checkpoints contain documentation only.

## Acceptance criteria

1. The environment and clean-checkout transcript show Git-captured
   `origin/main`, `HEAD`, and command results, with every required command
   labelled PASS, FAIL, or NOT RUN. Evidence: baseline transcript and worker
   note cite the exact commands and Git output.
2. The module/dependency, test, warning, and persistence inventories identify
   every examined source of truth and every persistence class without claiming
   unsupported durability. Evidence: linked inventory entries include files,
   tests, and known unknowns.
3. Each required public-boundary fixture is a literal committed artifact with a
   declared owner and source capture, and no fixture is generated or refreshed
   by the implementation it guards. Evidence: fixture manifest and reviewer
   inspection of the literal files.
4. Every ledger entry has a live evidence reference, one allowed classification,
   and an F-package or explicit defer disposition. Evidence: debt ledger review.
5. The complete branch diff changes only documentation and literal fixtures; no
   production code, test code, dependency, lockfile, or public contract changes
   exist. Evidence: complete branch diff and final Git status.
6. The final required matrix ran serially after the final artifact edit, all
   mandatory commands are PASS, and the task-packet checker passes. Evidence:
   transcript timestamps/order and worker-note verification table.

## Required verification

Run the following serially after the final F1 artifact edit. Record each result
as PASS, FAIL, or NOT RUN; a mandatory NOT RUN blocks COMPLETE.

```powershell
git fetch origin --prune
git rev-parse origin/main
git rev-parse HEAD
git status --short --branch
rustup show
cargo --version
cargo fmt --all -- --check
cargo check --all-targets --all-features --locked
cargo test --all-targets --all-features --locked
cargo clippy --all-targets --all-features --locked -- -W clippy::all
just verify
just verify-agent
pwsh -NoProfile -File .github/scripts/check-tethers-task-packet.ps1
git diff --check origin/main...HEAD
git diff --name-only origin/main...HEAD
git status --short --branch
```

## Forbidden changes

- No production, OCaml, Rust, PowerShell, CI, test-code, protocol, schema,
  dependency, `Cargo.lock`, public-visibility, CLI, Trail, replay, or recovery
  behaviour changes.
- No test hooks, generated compatibility fixtures, fixture auto-update command,
  or implementation-coupled golden-file refresh.
- No repair of a finding, including the candidate live-stderr issue.
- No weakening of this packet, no new product capability, and no scope cleanup.
- Commit and push only documentation/evidence/fixture changes on
  `foundation/f1-baseline`; do not merge, amend, tag, open a pull request, or
  begin F2.

## Stop conditions

Stop and report direct evidence if `origin/main` differs from the frozen
provisional baseline; the worktree/branch/base is unexpected; the tree is dirty
before F1 edits; a required claim needs a production/test change; a fixture
cannot be made independent of its generator; durability or compatibility is
uncertain; a required command fails; or two materially similar attempts fail.
Return one smallest unresolved question. Do not use a packet edit to bypass a
stop condition.

## Expected pre-existing changes

None.
